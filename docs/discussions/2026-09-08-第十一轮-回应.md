# 回应：架构评审（第十一轮）

- 回应人：dendro 开发者
- 日期：2026-09-08
- 对象：`2026-09-08-架构评审-第十一轮.md`
- 门槛：`cargo test --workspace` **212** 通过 / 0 失败；slt 9/9；clippy `-D warnings` 零输出

---

## 0. 总体态度

收尾总评接受：正确性防线**可以收敛**。三个天级收口（SHOW 一行、use_branch
封堵、SQL 侧回归转正 + Q-18 固化）本轮全部完成。评审抓到的"SHOW 声称已做
实际未落地"是 replace 静默失败的第三次——本轮起所有代码级 replace 都带
assert（事实：本轮两个 assert 各拦下一次静默失败）。

## 1. 三个收口 → ✅

1. **SHOW BRANCHES 事务内放行**：守卫排除 Show（带回归注释指向测试）。
   回归 `r10_show_branches_allowed_and_snapshot_lifecycle`——同时是
   **R10-1/R10-3 的 SQL 侧常驻回归**（COMMIT 注销断言 + 同 watermark
   引用计数断言），第十轮证据列引用 KV 测试充当 SQL 证据的失实就此
   更正。
2. **Kv::use_branch**：事务内拒绝（25001，与 SQL 侧 Q-11 同口径）——
   此前静默丢弃事务且不注销活跃快照（R10-4 接入后升级为注册表泄漏）。
   回归 `kv_use_branch_inside_txn_rejected`。
3. **Q-18 固化**：`tests/assembly.rs`——spawn 真实 dendro 进程，断言
   **端口↔协议对应**（PG Startup→AuthenticationOk、MySQL 握手、RESP
   PING→+PONG、metrics readyz→200），随 CI 跑。两次装配 P0 的结构性
   根因（装配层零测试）就此关闭。

## 2. 记账勘误（全单接受）

- README slt "7/7" → 9/9（M7 行）；TASK.md M7 行同步；
- 回应基线计数"208"实为 209（本轮起以脚本聚合计数为准）；
- kv.rs 头注 "SPEC 11" 缺号 → 标注 kv 面未单独成 spec（Q-15）；
- M-1（backup）/ M-2（格式守卫）已交付未记账 → TASK.md 划线入账；
- R9-6 已修（use_branch），R9-9/R9-11 保留登记。

## 3. 基线（本地实跑）

**212 通过 / 0 失败 / 1 ignored**；slt 9/9；clippy `-D warnings` 零输出。
新增回归 4：SHOW 放行 + SQL 侧生命周期（1）、use_branch 拒绝（1）、
装配冒烟（1）、kv_txn（第八轮已计）。

## 4. 收敛确认与第二阶段

按评审"可以收敛"的判定，主干正确性收官；第二阶段（不阻塞主干）：
AP 路径自身写 + 冻结根（Q-14）、真并发（P1-7）、真崩溃（P1-8）、
Q-9 树版本链增强、Q-12 优雅关闭、Q-1 读内存上界、Q-10 事务化 DDL、
Q-15 文档总清账。P2'（Adjudicator/Journal trait 化）按 SOTA 调研路线
继续推进。
