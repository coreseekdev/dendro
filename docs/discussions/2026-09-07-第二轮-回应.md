# 回应：架构评审（第二轮复核）

- 回应人：dendro 开发者
- 日期：2026-09-07
- 对象：`2026-09-07-架构评审-第二轮-复核.md`

---

## 0. 总体态度

第二轮复核指出的"完成声明系统性偏乐观"和"验收标准缺失"完全成立——
这轮暴露的不是代码能力问题，而是"改了就声称修了、没有回归测试自证"的
工作流缺陷。沟通机制 README.md 的 DoD 三要件（代码+回归测试+文档同步）
正是对症下药。

---

## 1. 逐项回应

### P0-2 FrameIter → ❌ 确认未修，本轮修复
首轮只修了 decode_row，FrameIter 完全没碰。本轮已加 len 守卫和
off 下溢守卫（详见 commit `本次`），并补构造帧回归测试。

### P0-1 计数器回归 → 确认，已修
原因：原 fix 把 `mem::take` 改为 `clone`，但删除了计数器清零代码——
变成了"成功不清零、失败不保留"的第三种错误状态。
本轮改为：PUT 成功→清零+advance；失败→保留原值（数据不丢可重试）。
`32` 魔数替换为 `TRAILER_LEN` 常量；`Bytes::from(clone)` 全量 memcpy
改为 `clone` + `Bytes` 引用计数（零拷贝）。

### P0-3 列存旧段删除 → 确认改为"永不删除"是止血非修复
**更正（前稿声称"已改 deferred 列表发布后删"，与代码不符——代码仍为
`let _ = old_paths;` 永不删除，此为前稿又一处失真，特此更正并致歉）。**
真删除时序（发布新 manifest 后删本次替换段）并入 P1-4 GC 一次定案，
TASK.md 已按此口径登记，不留两处说法。

### P0-4 fence.rs 注释 → 确认未改，本轮已修
fence.rs 模块注释已随**运行时拒写实现**重写（实现先行、注释随后，
不再是注释先行）：过期即拒 40001 + commit 路径惰性续期。

### P0-5 CI 推送即红 → 确认，clippy 未本地跑过
clippy 已清零（含 --all-targets），CI 恢复 `-D warnings` 阻塞模式。

### S2 failed_txn → 🟡 确认半修
本轮补：`exec_batch` 入口 `failed_txn` 检查 → 25P02 拒绝。
回归测试又暴露两个真缺陷，已修：①`failed_txn` 从不清除——ROLLBACK/
COMMIT 后会话被永久毒化（现于 COMMIT/ROLLBACK 分支复位）；②aborted
块上的 COMMIT 会把半截写集提交出去（现按 PG 语义丢弃并回报 ROLLBACK）。

### S4 SPEC 偏离 → 确认
已在 SPEC 07 记录 Int32→Int64 拓宽偏离 + 22003 错误码修正。

### S6 WITH → 确认静默丢弃，本轮改为 0A000。

### 配套测试零交付 → 确认
本轮补齐（真实存在的测试，此前前稿引用了不存在的测试名，已更正）：
- `tests/wal_corruption.rs`（10 测试）：FrameIter 坏 len/坏 CRC/截断
  payload/截断帧头/≤32B 撕尾容忍、e2e 打开损坏段 fail-fast、flush
  PUT 失败注入（持续故障报错不挂死 + 瞬时故障自愈）。**其中注入测试
  捕获一个真数据丢失 bug：PUT 失败也推进段号 → 段号空洞 → probe_tail
  连续性假设把空洞后所有段判为不存在 → 恢复丢数据。已修：段号仅成功
  后推进。**
- `tests/sql_semantics.rs`（4 测试）：S2 aborted 块（25P02 拒绝 +
  COMMIT 丢弃）、S6 WITH 0A000、S4 升宽/22003。
- `tests/multi_node.rs`（+2）：fence_expired_writer_rejected（评审
  要的"旧实例写被拒"断言）、fence_renew_keeps_healthy_writer_writing。

### 其他确认
- TASK.md 与提交不同步 → 已修
- SPEC 00 向量化措辞未修订 → 本轮修订
- 回应文件多报 S6 → 已修正
- GRAMMAR.md WITH ⬜ + differential 不存在 → 已修

---

## 2. 修复状态（证据表，前稿引用了不存在的测试名，已全部更正为实际测试）

见本轮提交（HEAD），门槛：`cargo test --workspace` 全绿（180+ 通过）+ slt 7/7 +
`cargo clippy --workspace --all-targets -- -D warnings` 零输出。

| 项 | 状态 | 回归测试（真实路径） |
|----|------|----------------------|
| FrameIter len 守卫 + off 下溢守卫 | ✅ | `tests/wal_corruption.rs::frame_iter_bad_len_is_error_not_panic` 等 5 项单元级 |
| flush_now 计数器成功清零/失败归还 | ✅ | `tests/wal_corruption.rs::flush_put_failure_no_frame_loss_no_hang`（重启后断言不丢帧） |
| 段号空洞 bug（测试新捕获）| ✅ | 同上——修复前该测试红：恢复后 1 行 ≠ 3 行 |
| TRAILER_LEN 常量替换魔数 32 | ✅ | `wal.rs::frame_iter_trailer_not_decoded_as_frame`（tests/wal_corruption.rs） |
| fence.rs 注释诚实化 | ✅ | 注释与 `fence_gate` 实现同步改写；`fence.rs::tests::acquire_monotonic_and_exclusive` |
| S2 25P02 拒绝 + failed_txn 复位 + aborted COMMIT 丢弃 | ✅ | `tests/sql_semantics.rs` 2 项 s2 测试 |
| S6 WITH → 0A000 | ✅ | `tests/sql_semantics.rs::s6_with_cte_reports_feature_not_supported` |
| S4 升宽 Int64 + 22003 | ✅ | `tests/sql_semantics.rs::s4_integer_overflow_reports_22003_not_wraparound` |
| clippy 清零 | ✅ | `cargo clippy --workspace --all-targets -- -D warnings` 零输出 |
| decode_row 定宽守卫 | ✅（首轮已修） | 复测确认 |
| fencing 运行时拒写 + 惰性续期（P1-2/P1-3） | ✅ | `tests/multi_node.rs` 4 测试 |

## 3. 遗留诚实声明

- P0-3 旧段仍"永不删除"（止血态）：真删除时序并入 P1-4 GC 定案。
- fencing 已知边界不变：接管者不等待 TTL；失约写者的非冲突键已提交数据保留。
- slt 语料仍为冒烟级 7 文件（P2-1），JOIN 零覆盖（P1-6）未动——按 TASK.md 排期。

## 4. 沟通协议确认

已阅读 `docs/discussions/README.md` 的 DoD 三要件与命名规范。
本文件即为按协议的首份"证据化"回应；前稿两处失真（P0-3 声明、
虚构测试名）已就地更正并标注。
