# 架构评审（第十七轮·轻量复评）：R16-1 修复 + P1-8 真崩溃——双双验真，G3 正确性级缺口闭合；新发现为账面纪律三则（无正确性项）

- 评审人：架构评审（外部）
- 日期：2026-09-08
- 对象：`7cede0d`（R16-1 回归改 NoWait + P1-8 `crash.rs` 真崩溃测试）+ R16-2/R16-3 登记声称
- 方法：轻量聚焦。R16-1 做**独立变异复跑**（禁用 `close_graceful` 的 flush → 红；还原 → 绿）；P1-8 做协议逐字节审查 + **灵敏度探针**（跳过 WAL 回放 → 应红在重启断言）；crash.rs 做 flake 评估（连跑 3 次）；clippy 门 + blanket allow 全仓 grep。终态 `git status` 干净、`git diff` 空（HEAD = `7cede0d`），变异/探针全部逐字节还原。
- 基线（评审人本机实跑）：`./scripts/count-tests.sh` → **workspace 220 / 0 / 1 ignored；slt 9/9**；`cargo clippy --workspace --all-targets -- -D warnings` **零输出**。与回应声称逐位一致（十五轮后 219 + crash 新增 1 = 220，账目自洽）。

---

## 0. 总体判定（TL;DR）

**两项交付均验真为真，本轮零新增正确性发现**：

1. **R16-1 验真 ✅**：`close_graceful_flushes_no_wait_tail` 已显式 `Durability::NoWait`（`wal_corruption.rs:474`）——评审人独立复跑第十六轮同款变异（flush 移除）→ **红**；还原（`git diff` 空）→ **绿 3/3**。红/绿闭合，Q-12 的防线自此真实且随 CI 常驻。十六轮的"修复一行"承诺兑现。
2. **P1-8 验真 ✅**：`crash.rs` 的自写 PG 客户端协议布局逐字节正确（§1.2）；SIGKILL 无清理；重启可见性断言经探针证明**真实依赖 WAL 回放**（跳过回放 → 红在 `crash.rs:177`）；Group ack 边界前提成立。真崩溃——G3 的最后一条正确性级缺口——首次有进程级实证。
3. R16-2/R16-3 登记为 ⚠️ **名义登记**：Q-12b/Q-12c 仅在 TASK.md 证据列被点名，无 ⬜ 队列行（R17-2）。

本轮新发现：**0×P0/P1/P2 + 4×P3（R17-1..4，全部账面/卫生级）**。连续第五轮零新增 P0。

---

## 1. 委托四项逐项核查

### 1.1 委托 a：R16-1 持久级确为 NoWait + 变异复跑 ✅

- 持久级：`wal_corruption.rs:473-474`——`o.durability = dendro_core::Durability::NoWait`，注释如实记录了十六轮根因（Group 下 exec 返回前缓冲必空）。缓冲真实存在，回归守护的对象自此存在。
- 变异 M-R17（评审人实跑）：`wal.rs:552-557` `close_graceful` 的 `flush_now` 禁用（`if false &&`）→ 测试 **FAILED**（"优雅关闭必须持久化 NoWait 缓冲尾"）；还原后 **PASS 3/3**（0.01s/次）。与 TASK.md:262 证据列声称的变异自检（禁用 → 红、还原 → 绿）完全一致。
- 判定：**R16-1 关闭**。十六轮 §2 建议的"变异自检写入证据列"已执行（TASK.md:262）；但 DoD 第 7 条常设化未落 README.md 文本（R17-4）。

### 1.2 委托 b：P1-8 crash.rs 协议正确性与 Group ack 边界 ✅

| 检查项 | 判定 | 依据（crash.rs） |
|---|---|---|
| Startup 布局 | ✅ | :62-68——len(4B 自含) + 196608（协议 3.0）+ `user\0postgres\0\0`（尾 NUL 终止），与 PG 规范一致 |
| Query 长度自含 +1 NUL | ✅ | :96-98/:107-109——`(body.len() + 4 + 1)`（4 为长度字段自身，+1 为 NUL），body 后补 `\0` |
| DataRow text 解析 | ✅ | :122-131——`nfields(2B) + collen(4B) + data`，`body[2..6]` 取首列长、`body[6..6+clen]` 解 ASCII 整型；ErrorResponse（'E'）即 panic（:133），错误不静默 |
| SIGKILL 无清理 | ✅ | :169-170——`child.kill()`（Unix 即 SIGKILL）+ `wait()` 收尸，无 graceful 路径 |
| 重启可见性断言真实 | ✅ | 探针 P-R17（评审人实跑，已还原）：`recovery.rs::replay_branch` 首行插 `return Ok(())` 跳过回放 → 测试 **FAILED at :177**（count=3≠5）——证明 4/5 两行确实走 WAL 回放、断言红在目标行为，非被 checkpoint 顺带满足 |
| Group ack 边界（ack 即 durable） | ✅ | serve 默认 `Durability::Group`（`main.rs:136`，测试未传 `--durability`）；exec → `wal.append` → Group 分支 `await_durable`（`wal.rs:389`，`engine.rs:1297` 调用点）在返回前完成上传，pgwire 在 exec 返回后才回 CommandComplete/ReadyForQuery——客户端见 'Z' ⇒ 5 行均已 durable。checkpoint 3 行 + 仅 WAL 2 行都应存活，断言 `count==5`（:177）与语义一致 |
| 崩溃后可继续写 | ✅ | :180-181——INSERT 第 6 行 + `count==6`，覆盖"重启后写路径健康"（新 epoch/租约重建） |

### 1.3 委托 c：crash.rs flake 评估——低风险，两处卫生项 ⚠️（R17-3）

- **实跑稳定性**：3/3 PASS，0.31s/次。
- 端口：`free_port()`（:11-17）bind-0 取_port 后释放存在 TOCTOU 窗口，属业界惯例、风险低；`--mysql/kv/metrics-port 0` 关闭多余监听（`main.rs:211/232` 的 `> 0` 守卫支持）缩小冲突面；即使 pg2 复用 pg1（child1 已死、端口已释放）也无冲突。
- 超时预算充足：wait_ready 10s（:43）、读超时 5s（:61）——卡死表现为 fail 而非 hang。
- **卫生项（R17-3）**：① 失败路径泄漏——:157 wait_ready 失败或 :177/:181 断言失败时，child1/child2 不被 kill、:185 目录清理被跳过，泄漏的 serve 进程与 /tmp 目录残留（cargo test 不代杀孙进程）；② 子进程 stdout/stderr 双双 `Stdio::null()`（:36-37），失败时无从诊断（探针 P-R17 红时恰好需要日志）。建议 RAII Drop 守卫 + stderr 落临时文件。不影响本批判定。

### 1.4 委托 d（R16-2/R16-3 登记核对）：⚠️ 名义登记（R17-2）

Q-12b/Q-12c 仅出现在 TASK.md:263-264 的证据列文本中，**任务清单内无对应 ⬜ 行**；原 Q-12 队列行（TASK.md:147）仍保留全范围口径（"停监听 → flush → exit 0 + /readyz live/readiness 分离"）未拆分未指向；P1-8 队列行（:41）仍 ⬜ 与 :265 的 ✅ 冲突。详见 R17-2。

---

## 2. 本轮发现（全部 P3）

1. **R17-1（P3）crash.rs 顶部 blanket allow——R15-1"零重现"状态一轮即破**：`crash.rs:1` `#![allow(clippy::all)]` 是全仓**唯一** blanket allow（grep 实证），恰在 R16 判定"blanket allow 零重现"的下一轮由新文件再引入——R14-2 模式第二次复发。评审人探针：删掉该行后 `cargo clippy -p dendro-server --all-targets` **零输出**——该 allow 当前零遮蔽，纯模板残留，删一行即恢复全域门槛。建议随下批回应交付，并把"新文件禁止 blanket allow"写进 CLAUDE/AGENTS 约定（连续两轮同一模式，制度没拦住）。
2. **R17-2（P3）登记账面三处失真**：① Q-12b/Q-12c 无 ⬜ 队列行——R16-2 的要害是"余项失去追踪"，现只解决到"余项有了名字"，扫描任务清单仍找不到它们；② P1-8 队列行（TASK.md:41）`⬜` 未随交付划掉，与 :265 `✅` 并存；③ TASK.md:263 "Q-12e 余项"为无出处笔误（"Q-12e"全仓仅此一处）。
3. **R17-3（P3，观察）**：crash.rs 失败路径进程/目录泄漏 + 子进程日志丢弃（§1.3）。
4. **R17-4（P3，脚注）**：① 十六轮建议的"DoD 增补第 7 条（变异自检常设化）"未落入 `docs/discussions/README.md` 完成定义（仍 3 条），目前仅 R16-1 证据列单点执行——R16-1 复发风险（R13-1→R16-1 已两次）靠自觉而非制度；② R16-4③ 未修：`main.rs:269` 注释"无信号设施的环境：随 join 阻塞至进程退出"仍失实（`Signals::new` 失败时闭包立即返回、线程结束，无 join 可言）。

---

## 3. G1–G7（第十七轮更新，仅列变化）

| 目标 | 十六轮 | **十七轮** | 依据与变化 |
|------|:----:|:----:|------|
| G1/G2/G5/G4/G6/G7 | — | **全部维持** | 本笔提交未触及对应主干；G4/G6/G7 仍因量化零工作 🟡 |
| G3 云原生 | 🟡（收窄） | **🟡（收窄至最后三件）** | 终止语义两半首次**都有实证**：优雅关闭（Q-12 + R16-1 修复后真实回归）+ 进程死亡（P1-8，探针证明断言真实）。剩余不上调原因仅为运维完备性：M-8 部署 yaml、M-5 直方图、Q-12b（停监听排空 + live/readiness 分离）/Q-12c。**M-8 + Q-12b 落地即可判 ✅** |

**结论**：主干正确性防线连续第五批次无回弹；G3 自"硬伤 → 证据缺口 → 证据闭合"的收敛轨迹完整。剩余工作全部是运维/量化类，无已知正确性洞。

---

## 4. 下一批最值得做的 3 件事

**先于三件事（随本轮回应交付，分钟级）**：R17-1 删 `crash.rs:1` 一行（clippy 探针已证零遮蔽）；R17-2 三处账面修正（:41 P1-8 划掉、:147 拆出 Q-12b/Q-12c 为 ⬜ 行、:263 笔误）；R17-4① DoD 第 7 条写入 README.md 完成定义。

| 序 | 项 | 排序理由 |
|:--:|------|------|
| 1 | **Q-1 读路径内存上界**（+ LIMIT 下推） | 十六轮第 2 顺位原样递补。读路径是仅存的无界资源面（执行器全程物化 + active_snaps 全量保留），Q-13 的保留精细化依赖 Q-1 先定读侧口径——联动定案防返工。产品级"资源安全"叙事的最后一块 |
| 2 | **M-3 最小压缩测量曲线**（顺带喂 P2-3） | G4/G6/G7 连续十七轮 🟡 的直接原因仍是"量化零工作"。一条最小 zstd 级别 × 列类型 Q 曲线进 `dendro bench` 同时解 M-3 与 P2-3 两账 |
| 3 | **M-8 最小部署 yaml + Q-12b**（G3 收尾件） | G3 距 ✅ 只差运维完备性：一份 Deployment yaml（SIGTERM 语义现成）+ 停监听排空 + live/readiness 分离。同批完成则 G3 可判 ✅，云原生三件套（优雅 + 崩溃 + 部署）证据链闭合；Q-12c（join checkpoint 线程）可随后或并入 |

未入选说明：Q-13 随 Q-1 联动不单独排批；Q-10 随 v2 catalog；P2'-2 受制 P3-1；P2-1 slt 语料仍为任意批次填充项。

---

## 附：评审方法说明

**本机实跑**（HEAD = `7cede0d`，评审终态 `git status` 干净、`git diff` 空、变异/探针全部还原）：

- `./scripts/count-tests.sh`：**workspace 220 / 0 / 1 ignored；slt 9/9**
- `cargo clippy --workspace --all-targets -- -D warnings`：**零输出**
- `cargo test -p dendro-core --test wal_corruption close_graceful_flushes_no_wait_tail`：PASS 3/3
- `cargo test -p dendro-server --test crash sigkill_crash_recovery_via_pg`：PASS 3/3（0.31s/次）

**变异与探针记录（均已还原）**：

| 编号 | 内容 | 结果 |
|---|---|---|
| M-R17 | `wal.rs::close_graceful` 禁用 flush_now（`if false &&`） | **FAILED**（"优雅关闭必须持久化 NoWait 缓冲尾"）→ R16-1 回归灵敏度实锤；还原后绿 3/3 |
| P-R17 | `recovery.rs::replay_branch` 首行插 `return Ok(())`（跳过 WAL 回放） | **FAILED at crash.rs:177**（count=3≠5）→ P1-8 断言真实依赖回放；还原后绿 |
| R17-P1 | 删 `crash.rs:1` blanket allow 后跑 clippy | **零输出** → 该 allow 当前零遮蔽（纯残留）；已还原 |

**核对过的材料**：`7cede0d` 全量 diff；`crash.rs` 全文（协议字节布局逐项对 PG v3 规范）；`wal_corruption.rs:460-490`；`wal.rs:380-412, 541-557`（append/await_durable/close/close_graceful）；`engine.rs:1297`；`recovery.rs:33-45`；`main.rs:130-136, 174, 211, 232, 253-269`；TASK.md:41, 147, 258-266；README.md DoD 节；十六轮报告/回应全文；全仓 blanket allow grep。
