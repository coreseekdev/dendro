# 架构评审（第二十轮·里程碑复评）：Q-10 守卫与 010 语料双双验真（变异红/绿闭合），零 P0/P1/P2；但 TRUNCATE 漏网守卫、"runner 不支持 error 断言"前提失实、commit message 计数再度失准——五项新发现全部 P3

- 评审人：架构评审（外部）
- 日期：2026-09-08
- 对象：提交 `56027ff`（Q-10 事务内 DDL 保守守卫 + P2-1 `010_cursors_txn.slt` + BASELINE 更新）+ 回应提交 `5665ee6`（第十九轮中断转自查的补完）
- 方法：轻量聚焦。委托四点（Q-10 覆盖面/误伤；010 实际执行路径；十九轮三条自查结论补验；G1–G7 总评）逐项核查 + 本机实跑 + 5 个探针 + 1 个变异，全部用后删除、终态 `git status` 干净。
- 基线（评审人本机实跑，HEAD = `56027ff`）：`./scripts/count-tests.sh` → **workspace 227 / 0 / 1 ignored；slt 10/10**；`cargo clippy --workspace --all-targets -- -D warnings` **零输出**。

---

## 0. 总体判定（TL;DR）

本批两项交付**均验真为真**：Q-10 守卫位于 `exec_statement` 分发之前（`sql/mod.rs:441-450`），回归经评审独立变异复跑红在目标测试、还原后绿（DoD 第 7 条闭合）；010 语料经 slt runner **真实驱动引擎**（非跳过），10/10 通过。第十九轮三条自查结论①②③**全部验真**。本轮**零 P0/P1/P2**，五个新发现全部 P3（守卫遗漏 TRUNCATE / commit message 计数 224 实为 227 / "runner 不支持 error 断言"失实 / DECLARE 多语句残留 / Q-10 记账未同步）。

---

## 1. 委托四项逐项核查

### 1.1 委托 a：Q-10 守卫覆盖面——核心验真 ✅，TRUNCATE 漏网 ⚠️

| 检查项 | 判定 | 依据 |
|---|---|---|
| 守卫位置与语义 | ✅ | `exec_statement` 入口先于分发（`sql/mod.rs:445-450`），`sess.txn.is_some() && is_ddl(stmt)` → 25001；注释引用 R18-2 实证与 v2 放开条件 |
| CREATE TABLE/INDEX、ALTER TABLE、DROP | ✅ | `is_ddl`（`sql/mod.rs:430-438`）；**`Statement::Drop { .. }` 通配所有 object_type**——DROP INDEX/VIEW 等虽引擎本不支持，也被守卫统一拒（无侧漏） |
| **TRUNCATE** | ❌ R20-1(P3) | 不在 `is_ddl`；`truncate_impl`（`ddl.rs:273-302`）**无条件调用 `catalog_commit`（:300）**——catalog commit 立即发布到 `b.head` + WAL CheckpointRecord + manifest `covered_seq`（:194-212），恰是 Q-10 拒绝 DDL 的同款理由。探针 P-R20-1：BEGIN 内 TRUNCATE **被接受**；行级墓碑入事务写集（回滚后 5 行全回），事务内 TRUNCATE+INSERT+COMMIT 正常（count=1，无 40001 连锁）——**无数据级漂移，属口径不一致 + 事务中段 catalog/WAL 发布**，修复一行（Truncate 加入 is_ddl）+ 回归 + 变异 |
| CREATE VIEW | ✅（非问题） | 引擎整体不支持（探针：事务内 → 42601 unsupported，无 catalog 写）——无漂移可言；登记：未来支持 CREATE VIEW 时必须同步入 `is_ddl` |
| 误伤（临时表/DML） | ✅ 无误伤 | 临时表引擎全程不支持（事务内外都建持久表——v1 既有口径，非守卫引入）；探针 P-R20-4c：事务内 INSERT/DELETE/SELECT 全通（UPDATE 主键列 0A000 为既有限制，与守卫无关） |
| 回归真实性 | ✅ | `q10_ddl_inside_explicit_txn_rejected`（`sql_semantics.rs:453-487`）：逐事务 25001 + 25P02 aborted 语义 + 事务内 DML 放行对照。**变异 M-R20-1（评审人实跑，已还原）**：守卫改 `if false` → 该测试 **FAILED**（"事务内不应允许"）；还原 → PASS。红/绿闭合 |

### 1.2 委托 b：010 语料执行路径——真实驱动 ✅，"error 断言不支持"前提失实 ❌

- **执行路径** ✅：slt runner（`crates/slt/src/main.rs:18-50`）对每条 `statement ok`/`query` 记录调 `sess.exec` → `exec_batch` → **游标语句在 `:84` 被拦截进 `exec_cursor_statement`**（DECLARE/FETCH/CLOSE 全走引擎）；`query IT` 记录的期望行由 runner 逐行比对，三次 FETCH 输出递进不同——通过即证明引擎位置推进真实。本机实跑 10/10。
- **"runner 不支持 error 断言"** ❌ R20-3(P3)：commit message 与 010 文件头均如此声明。**探针证伪**：sqllogictest 0.28 支持 `statement error` 且双向强制——成功语句标 `statement error` → `FAIL ... statement is expected to fail, but actually succeed`；报错语句标 `statement error` → 通过。34000/25001/25P02 路径**现在就能**入 slt 语料，P2-1 续作的规划前提需更正。

### 1.3 委托 c：十九轮三条自查结论补验——三条全部验真 ✅

| # | 声称 | 判定 | 依据 |
|---|---|---|---|
| ① | DoD 第 4-7 条已落 discussions/README.md | ✅ | `README.md:13-17`：grep 命中 / 脚本计数 / replace assert / 变异自检四条齐备，且各自注明教训来源 |
| ② | TASK.md Q-1 拆分 Q-1a/b/c | ✅ | `TASK.md:120`（Q-1a ✅，标题含"WHERE 非空禁用"——R18-1 修复同步入账）、`:121`（Q-1b ✅）、`:122`（Q-1c ⬜） |
| ③ | Branch::stopping 死字段已删 | ✅ | `engine.rs` Branch 结构（:116 起）无 stopping 字段；仅存 `Database.stopping`（:355）与 `is_stopping/begin_stopping`（:1091-1101）——与 Q-12b 消费点一致 |

顺带验真：R18-2/R18-6 勘误已落地——`bench/mod.rs:383` 去 `*2`（注释引 arrow 59.3 求和语义）、单位改 `enc_mrows_s/dec_mrows_s`；TASK.md:104 M-3 行已按真实口径（1.06/0.97/3.91）登记。R18-9④（metrics 非 200 reason 恒 "Not Found"，`metrics.rs:90`）仍未动——P3 观察项在册。

### 1.4 委托 d：G1–G7 里程碑总评

| 目标 | 十八轮 | **二十轮** | 依据与变化 |
|------|:----:|:----:|------|
| G1 协议/实现分离 | ✅ | **✅ 维持** | 未触及 |
| G2 只写+git 式分支 | ✅ | **✅ 维持** | 未触及 |
| G3 云原生 | 🟡（收窄至 M-8） | **🟡 维持** | Q-10/010 不触及主干；**M-8 部署 yaml 落地即可判 ✅** |
| G4 列存 GPU 保留 | 🟡 | **🟡 维持** | 机制（CBF/codec 分层）在；M-4 GPU 解码对拍仍未动；bench 数字勘误后可引用但仅 3 codec × 单一合成批 |
| G5 SQL 一等公民 | ✅ | **✅ 终判维持（本批进一步加固）** | 委托所称"上轮定义距 ✅ 仅差 Q-14 与 Q-16/Q-5"为第十四轮口径，**G5 已于第十五轮上调 ✅**（队列与 TASK.md R15-1 在案），十六/十八轮维持。本轮复核无回弹：语料 9→10 文件、Q-1b/Q-10 语义有变异敏感回归、语法面（游标族）入 GRAMMAR ✅ 行。残余（Q-16 事务内重复键、Q-5 SET 假象、R20-1 TRUNCATE 口径）均为边角，不阻门 |
| G6 压缩量化 | 🟡（数字作废） | **🟡（数字修复，可引用）** | 双计/单位已修（§1.3 顺带验真）；完整曲线（zstd 级别 × 列类型）随 P2-3/M-3 扩展 |
| G7 性能绝对优先 | 🟡 | **🟡 维持** | clippy 零输出、红线守则未破；P2-6 热路径优化与 P2-3 证据链未动 |

---

## 2. 本轮发现（按严重度，全部 P3）

1. **R20-1（P3）TRUNCATE 漏网 Q-10 守卫 + 事务中段 catalog/WAL/manifest 发布**：`is_ddl`（`sql/mod.rs:430-438`）缺 `Statement::Truncate`；`truncate_impl:300` 无条件 `catalog_commit`（head 前移 + CheckpointRecord + `covered_seq`，`ddl.rs:194-212`）。探针实证：事务内接受、行级部分可回滚、无数据漂移——与 Q-10 自身口径不一致。修复一行 + 回归（按 DoD 第 7 条附变异）。
2. **R20-2（P3，声称与事实不符）commit message 基线计数失准**：`56027ff` 声称 "Script-counted baseline: **224** workspace tests"——评审人实跑脚本为 **227**（恰为 HEAD 的 `#[test]` 属性数：把数属性当成了跑脚本）。slt 10/10 属实。这是 DoD 第 5 条设立以来计数失准的**第五次**（R13-2 后又犯），且发生在明言"script-counted"的提交里。
3. **R20-3（P3）"runner 不支持 error 断言"前提失实**：sqllogictest 0.28 原生支持 `statement error` 且双向强制（§1.2 探针）。010 头注与 commit message 的这一表述需勘误；P2-1 续作应直接把 34000/25001/25P02 断言迁入 slt。
4. **R20-4（P3）R18-4 修复残留：DECLARE 多语句 query 体仍先执行后丢弃**：`mod.rs:969-973` 的 SELECT 前缀检查只挡单条 DML 形态；探针 P-R20-3：`DECLARE c CURSOR FOR SELECT id FROM t; INSERT INTO t VALUES (99)` → 游标照常声明（1 行），**INSERT 副作用已发生**（count=2）。R18-4（P3）未完全闭合，勿记 ✅。
5. **R20-5（P3，记账/DoD 第 3 条）Q-10 交付零文档同步**：TASK.md `:139`（R8-P2 行）与 `:147`（Q-10 行）仍为 ⬜、"catalog 写集事务化（v2）"原口径——保守拒绝方案已交付却无一行登记；SPEC 07 与 GRAMMAR.md 均无"显式事务内 DDL → 25001"（grep 零命中）。未来读者会视 Q-10 为未做。

---

## 3. 下一批最值得做的 3 件事

| 序 | 项 | 排序理由 |
|:--:|------|------|
| 1 | **Q-1 读路径内存上界**（游标 map 上界 + R18-5 口径文档化），顺带分钟级清账批：R20-1 一行修复（Truncate 入 is_ddl + 回归 + 变异）、R20-2/3/5 勘误与 TASK/SPEC/GRAMMAR 同步、R20-4 补判 | 十八轮顺位 3 递补至今未动，游标物化使读侧无界面 +1（与 Q-13 联动防返工）；清账批成本分钟级，防"声称-事实"模式复发 |
| 2 | **P2-1 真正续作：error 断言入 slt + 上游语料接入**（sqllogictest-corpus 方言子集哪怕 1%，兑现 P2-1 原始目标）+ Q-17（runner 显式 checkpoint_interval_s=0） | R20-3 证伪后路径已通；G5 的证据分从"自写语料"升级为"上游兼容性" |
| 3 | **M-8 部署 yaml 入库**（k8s Deployment/Service/readyz 探针配置 + 部署文档） | G3 ✅ 的最后一件，纯文档/配置工作量，里程碑收益最大 |

未入选说明：M-3 完整曲线（zstd 级别 × 列类型）随 P2-3 证据链一并推进（二者共享 bench 基建）；P2-2 向量化执行器为 G4 深水区，宜在 P2-3 定标尺后动工。

---

## 附：评审方法说明

**本机实跑**（HEAD = `56027ff`，终态 `git status` 干净、探针/变异全部还原）：

- `./scripts/count-tests.sh`：**workspace 227 / 0 / 1 ignored；slt 10/10**
- `cargo clippy --workspace --all-targets -- -D warnings`：零输出

**探针与变异记录（均已删除/还原）**：

| 编号 | 内容 | 结果 |
|---|---|---|
| P-R20-1 | BEGIN; TRUNCATE t; 查行数; ROLLBACK; 查行数 | **TRUNCATE 被接受**；事务内 count=0，回滚后 count=5（行级可回滚）→ R20-1 实锤 |
| P-R20-2 | BEGIN; TRUNCATE; INSERT(9); COMMIT | 全 OK，final count=1（无 40001 连锁——发布为显式拒绝类盲区未触发） |
| P-R20-3 | `DECLARE c CURSOR FOR SELECT...; INSERT ...VALUES(99)` | 游标声明成功（FETCH 1 行）且 **count=2** → R20-4 实锤 |
| P-R20-4 | 事务内 CREATE VIEW / DROP INDEX / INSERT/UPDATE/DELETE/SELECT | CREATE VIEW → 42601（不支持，无 catalog 写）；DML 全通（UPDATE pk 0A000 为既有限制）→ 无误伤实证 |
| P-R20-5 | `statement error` × 成功语句 / 报错语句；`statement ok` × 报错语句 | 成功语句报 error 期望 → **FAIL "expected to fail, but actually succeed"**；报错语句报 error 期望 → 通过 → R20-3 实锤 |
| M-R20-1 | 禁用 Q-10 守卫（`if false &&`） | `q10_ddl_inside_explicit_txn_rejected` **FAILED**；还原 → PASS → 回归灵敏度实锤 |

**核对过的材料**：`56027ff`/`5665ee6` 全量 diff；`sql/mod.rs:68-100, 429-450, 875-1026`；`ddl.rs:159-302`；`sql_semantics.rs:439-487`；`crates/slt/src/main.rs` 全文；`010_cursors_txn.slt`/`BASELINE.md`；`engine.rs:116, 355, 1091-1101`；`bench/mod.rs:370-420`；`metrics.rs:90`；TASK.md `:104, :120-122, :139-147`；discussions/README.md `:13-17`；sqllogictest 0.28 runner 行为（探针实证）。
