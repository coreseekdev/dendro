# 架构评审（第十一轮·收尾轮）：R10 修复复核（三路径注销 / 按名装配 / 引用计数 / KV 接入）+ 收尾总评（G1–G7 终评、未完项总排序、收敛判定）

- 评审人：架构评审（外部）
- 日期：2026-09-08
- 对象：`2026-09-08-第十轮-回应.md`、TASK.md（R10 段 + Q-18/19/20）、提交 `7fe47dc`（第十轮修复）
- 方法：逐行读新代码 + 本地实跑 + **九探针 + 两组进程级实验**（探针文件与全部临时产物已删除，评审后 `git status` 干净，见文末方法说明）。委托点 a–e 逐项核查；按委托完成**收尾总评**：G1–G7 终评（§4.2）、TASK.md 全部未完项优先级总排序（§4.3）、"下一批最值得做的 3 件事"（§4.4）、写/读/装配/生命周期正确性防线的**收敛判定**（§4.5）。
- 基线（评审人本机实跑，HEAD = `7fe47dc`，探针删除后工作树干净）：`cargo test --workspace` **209 通过 / 0 失败 / 1 ignored**（回应写"208 通过"，实际 209——新增 1 测试后未更新口径，小账）；slt **9/9**；**`cargo clippy --workspace --all-targets -- -D warnings` ✅ 零输出**（上轮失守的门槛恢复）；`cargo build --release` 成功（进程级实验用）。

---

## 0. 总体判定（TL;DR）

**第十轮的五项修复全部验真为真**——这是第十轮双 P0 以来第一次"声称即事实"的修复批次：三路径注销完备（含 failed-COMMIT 的两个子路径）、按名装配在默认与四端口组合下经进程级实验全部正确、引用计数在并发同快照 + 异序结束下语义正确、KV 显式事务接入两套机制且冻结读成立、covered_min 条件推进使 Q-9 语义更准。**本轮未发现任何新的 P0——十轮以来首次。**

两处残留：

1. **P11-1（P1）：`Kv::use_branch` 静默丢弃活跃事务且不注销（`kv.rs:66-72`）**——R10-4 把 KV 事务接入注册表后，这条已登记未修复的 R9-6（Q-20 补录）从"静默丢事务"升级为"**注册表泄漏 → 该分支截断永久跳过 → memtx 无界**"：探针 P11-7 实锤，`begin()` 后 `use_branch("b2")`，main 分支注册表 `{0:1}` 在会话 Drop 之后**永久残留**。触发条件比 R10-1 窄（需 KV 会话持活跃事务切分支），故 P1 而非 P0，但后果与 R10-1 同级。
2. **P11-2（P2）：回应、commit 信息、TASK.md 三处声称"SHOW BRANCHES 放行已完成"——代码里不存在**。守卫（`sql/mod.rs:257-264`）在 `796267c..7fe47dc` 的 diff 中零改动，仍对全部 BranchKind 一刀切；探针 P11-6 实锤：事务内 `SHOW BRANCHES` → 25001。十轮评审 R10-6 原样未动，但被记成 ✅。

收尾总评的结论（详见 §4.5）：**写路径、读路径（行式 + KV 点查/扫描）、装配与生命周期的正确性防线——可以收敛。** 条件是三个天级收口（SHOW 一行修复、use_branch 封堵、SQL 侧回归转正）。未收敛的域是 AP 列存路径（Q-14）、真并发防线（P1-7）与真崩溃防线（P1-8）——它们属于第二阶段，不属于主干收口。

---

## 1. 第十轮修复声称——逐项抽查 verdict

判定词：✅ 已验证 · 🟡 部分 · ❌ 声称与事实不符

| 声称（回应/commit/TASK.md） | verdict | 证据 |
|------|:-------:|------|
| R10-1：COMMIT / ROLLBACK / failed-COMMIT 三路径注销 | ✅ | COMMIT `sql/mod.rs:460`、failed_txn-COMMIT 丢弃 `sql/mod.rs:452-456`、ROLLBACK `sql/mod.rs:467-471`、Session::drop 兜底 `engine.rs:1203-1207`。探针 P11-1/3：四路径后 `active_snaps` 皆空；**COMMIT 后再 checkpoint 截断真实发生**（covered_min 0→covered，上轮 P10-1 场景关闭）。commit_tx 失败路径（fence 40003 / Q-9 40001）也注销——unregister 在 `commit_tx` 之前（`sql/mod.rs:460-462`、`kv.rs:322-324`），探针 P11-4/8 实锤 |
| R10-2：listeners 改 HashMap 按名存取 | ✅ | `main.rs:173-185`（按名 insert）+ `:189/201/214/234`（按名 remove）。进程级实验（两组、四端口逐一探判）：默认组合 pg 端口 = PG（SSLRequest→'N'、startup→'R' AuthenticationOk）、mysql 端口 = MySQL 服务器先发握手（`8.0.36-dendro`）；四端口全开时 kv 端口 = RESP（PING→`+PONG`）、metrics = HTTP 200。**banner 与实际监听首次一致**；bind 冲突第三实例仍 exit=1 无 ready（R9-4 行为未回退） |
| R10-3：`BTreeMap<u64, usize>` 引用计数 | ✅ | 注册 `entry().and_modify(+1).or_insert(1)`（`sql/mod.rs:434-438`、`kv.rs:310`）；注销递减、归零摘除（`engine.rs:1208-1215`、`kv.rs:335-345`）。探针 P11-2：同 watermark 双会话 BEGIN → 计数 2；s1 ROLLBACK 先走 → 仍 1；其间并发提交 + checkpoint → 截断跳过（covered_min 保持 0）、s2 冻结读完好；s2 COMMIT → 空 → 截断恢复。**上轮 P10-2 击穿场景关闭** |
| R10-4：KV 显式事务接入两套机制 | ✅（附新洞） | `Kv::begin`（`kv.rs:300-316`）持 commit_mu 取快照 + 注册 + 冻结 head_root；`kv_entry` 显式事务以冻结根 `catalog_lookup`（`kv.rs:347-366`）；get/scan 三层覆盖不变（`kv.rs:114-124/205-238`）；commit/rollback/Drop 注销（`kv.rs:317-332/486-492`）。回归 `kv_txn_frozen_reads_and_registry_lifecycle`（`kv_wire.rs:118-150`）+ 探针 P11-8/9（commit_tx 失败注销、KV+SQL 跨层同快照计数 2）。**但 use_branch 漏洞被 R10-4 激化（§0 第 1 条、§3 P11-1）** |
| P10-5：covered_min 只在真截断时推进 | ✅ | `engine.rs:1071-1078`：`if !has_active { truncate_all; covered_min.store }`。探针 P11-5：活跃事务期间他人 checkpoint → covered_min 保持 0、保留窗口内**真冲突由 OCC 裁决 40001**（validate_only 对完整 memtx 历史）、非冲突事务正常提交——上轮 P10-5 的"消息失实"场景消除。r7_3/q9/r9_1 回归仍绿；注意 **q9 现在经 OCC 路径拒绝**（活跃事务注册在先 → 截断被跳过 → covered_min=0 → 拒绝只能来自 validate_only）——语义更准且测试实际变强，但这一机制变化未写入任何文档（Q-15 范围） |
| clippy 两个 unused 修复 | ✅ | 本机实跑 `-D warnings` 零输出；`main.rs` 死变量已删 |
| "R9-2 守卫放行只读 SHOW BRANCHES" | ❌ | **三处声称（回应 §2、commit 信息、TASK.md R10-P2 行"✅ Show 放行"）与代码不符**：守卫 `sql/mod.rs:257-264` 对 `branch_sql_kind` 全返回值一刀切，`git diff 796267c..7fe47dc -- sql/mod.rs` 无此 hunk；探针 P11-6：事务内 `SHOW BRANCHES` → 25001 "cannot execute SHOW BRANCHES inside a transaction"（第十轮 P10-4 原样） |
| 登记 Q-18/Q-19/Q-20 | ✅ | TASK.md:184-187 逐条可查；AGENTS.md 红线如实登记为待办未虚报完成。两处小失准：基线"208 通过"实为 209；TASK.md R10-P0 证据列引用**不存在的测试名** `kv_txn_frozen_reads_and_decoded_values`（现名 `kv_txn_reads_own_writes_and_decoded_values`，且它不断言注册表——带断言的是 `kv_txn_frozen_reads_and_registry_lifecycle`，且那是 KV 侧测试，SQL 侧 P0 无回归，见 §3 P11-3） |

**小结：9 项声称 7 真 / 0 部分 / 2 失实**（SHOW 放行、证据列测试名）。与十轮（4 真/3 部分/2 失实，含两个行为级 P0 假声称）相比，修复质量实质上升：本轮的失实是 P2 功能项的记账失实，不再有行为级假声称。

---

## 2. 专项审查（委托点 a–e）

### 2.1 委托点 a：三路径注销的完备性 + 引用计数语义

**注销面全图**（探针 P11-1/3/4/8/9 逐一验证）：

| 路径 | 注销点 | 验证 |
|------|------|:----:|
| SQL COMMIT（成功） | `sql/mod.rs:460`，在 commit_tx 之前 | ✅ |
| SQL COMMIT（aborted 丢弃，25P02 后） | `sql/mod.rs:452-456` | ✅ |
| SQL COMMIT（commit_tx 失败：40003/40001） | 同上（unregister 前置 → `?` 上抛时已注销） | ✅ P11-4 |
| SQL ROLLBACK | `sql/mod.rs:467-471` | ✅ |
| SQL Session::drop（未决事务） | `engine.rs:1203-1207` | ✅ |
| KV commit（成功/失败） | `kv.rs:322` | ✅ P11-8 |
| KV rollback / Drop | `kv.rs:328-332/486-492` | ✅ P11-9 |
| **KV use_branch（活跃事务）** | **无——`self.txn = None` 静默丢弃** | ❌ P11-7 |

**引用计数语义**（探针 P11-2）：同 watermark 并发事务各持一槽（计数 2）→ 异序结束（先 ROLLBACK / 后 COMMIT）→ 保护随最后一个持有者解除 → 空后截断恢复。十轮 P10-2 的"第三方会话解除保护"与十轮 P10-1 的"提交后永久占用"两个端到端击穿场景均关闭。

**竞态口径（上轮 R10-7）**：SQL BEGIN（`sql/mod.rs:431`）与 KV begin（`kv.rs:306`）现均在 commit_mu 内完成"取快照 + 注册"；checkpoint 的 has_active 判定 + 截断 + covered_min 推进全程在 commit_mu 内（`engine.rs:847-849` → `:1075-1078`）。注册与截断评估互斥，**竞争窗口结构性闭合**。锁序一致（commit_mu → active_snaps，无反向嵌套），无死锁面。代价：BEGIN 的延迟上界 = 一次 checkpoint 全程（含远端 PUT）——G7 相关，量级可接受，建议注释声明（P3）。

**SQL 侧回归缺口**：`sql_semantics.rs` 本轮零新增——上轮强调的"COMMIT 后截断必须恢复""同快照双会话异序结束"两个行为**没有常驻回归**，本轮仅由评审探针覆盖。R10-1/R10-3 的 SQL 修复目前靠"改对了"在防退化，不靠测试（§3 P11-3）。

### 2.2 委托点 b：端口↔协议对应（进程级）

| 端口 | banner 声称 | 实测行为 | 判定 |
|------|------|------|:----:|
| 18342（默认组合） | pg | 静默 → SSLRequest 'N' → startup 'R' AuthenticationOk | ✅ PG |
| 18306（默认组合） | mysql | 服务器先发 MySQL 握手 `8.0.36-dendro` | ✅ MySQL |
| 18343（四端口） | pg | 同上 'R' | ✅ PG |
| 18307（四端口） | mysql | 同上握手 | ✅ MySQL |
| 18379（四端口） | kv | RESP `PING` → `+PONG` | ✅ RESP |
| 18469/18470 | metrics | `GET /readyz` 200、`/metrics` Prometheus 文本 | ✅ HTTP |
| （bind 冲突复验） | — | 第三实例占用 pg 端口 → `dendro: pg listen ... failed: Address already in use`，exit=1，无 ready | ✅ |

README 快速开始（psql→5432）在默认配置下恢复可用。**Q-18（装配冒烟测试自动化）仍未落地**——本轮验证仍是人工的，防线未固化（§4.4 第 1 件事的核心内容）。

### 2.3 委托点 c：covered_min 条件推进后的 Q-9 语义

- **真截断 → 盲区兜底仍在**：`commit_tx` 的 `snapshot < covered_min → 40001`（`engine.rs:1261-1264`）保留；探针 P11-4 人为制造盲区（covered_min 抬高）→ COMMIT 40001 且注册表注销。正常流水中该兜底现在几乎不可达（活跃事务必被注册 → 截断必被跳过）——这正是"40001 退役为真盲区兜底"的预期形态。
- **有活跃快照 → 完整 memtx 历史 → OCC 正常检测**：探针 P11-5——保留窗口内同键冲突提交被 validate_only 以 40001 拒绝（first-committer-wins 对完整版本链生效），异键事务正常提交。**比此前的"无差别必拒"严格更优**：误拒消失、真冲突不漏。
- **回归面**：q9（`sql_semantics.rs:208`）仍绿，且**拒绝机制已从"盲区必拒"切换为"OCC 实裁"**（活跃事务注册在先 ⇒ covered_min=0 ⇒ 唯一可能的拒绝来源是 validate_only）——q9 无意间变成了 OCC 的行为测试，防线反而变强。但该机制切换未记录于 SPEC 04 / TASK.md（Q-15 范围扩大：SPEC 04 §3 的提交伪代码仍是最初版，与 commit_tx 管线脱节已久）。
- Q-13（按版本精细化保留）维持 ⬜：活跃快照存在即全量跳过截断的保守口径未变，内存随最老事务增长——已知权衡，不再重复计分。

### 2.4 委托点 d：KV 冻结读与失败路径

- **冻结根解析**：`kv_entry` 显式事务走 `catalog_lookup(冻结根)`（`kv.rs:351-360`），与 SQL 侧 R7-3 同口径；`KV_TABLE`（`__kv`）无后缀，rsplit 取短名逻辑与 `resolve_table` 等价。get 的"写集→memtx@快照→树@冻结根"与 scan 的"树→overlay→写集最后覆盖"层级未变（R9-5 验真结论沿用）。
- **生命周期**：commit/rollback/Drop 注销（P11-9）；commit_tx 失败注销（P11-8）；`kv_txn_frozen_reads_and_registry_lifecycle` 覆盖"冻结读 + COMMIT 后注册表空 + 新会话可见"主链。
- **唯一洞 = use_branch**（§3 P11-1）。`cas()` 内部的 begin/commit/rollback 自闭合，不泄漏。
- KV 遗留记账：kv.rs 模块注释宣称"SPEC 11"——**spec/ 目录不存在 SPEC 11**，KV 仍是三条 wire 中唯一零规格的一条（十轮 ❌ 维持，Q-15 扩项）。

### 2.5 委托点 e：clippy 恢复

`cargo clippy --workspace --all-targets -- -D warnings` 零输出（本机实跑）；上轮两处 unused（`main.rs:199/233`）已随 R10-2 的按名改造自然消除。CI clippy job（`.github/workflows/ci.yml`）在 HEAD 可绿。门槛恢复，但"本地三合一门禁（test+slt+clippy）前置"的建议十轮提出后仍无流程化载体——连续两轮依赖提交者自觉。

---

## 3. 新增缺陷与工程问题清单（本轮发现）

### P1

1. **P11-1 `Kv::use_branch` 静默丢活跃事务 → 注册表泄漏 → 分支截断永久跳过（`kv.rs:66-72`，探针 P11-7）**：`begin()` 后 `use_branch("b2")`，旧分支注册表条目永久残留（会话 Drop 也不清——txn 已被置 None）。R10-4 接入注册表使已登记未修的 R9-6 后果升级一级。修法（二选一，与 SQL 对齐为优）：① 活跃事务内 use_branch → 25001（SQL 已是此语义，`sql/mod.rs:268-271`）；② 切换前 `unregister_snapshot` 再丢事务。回归：use_branch 后旧分支 `active_snaps` 必须空 + 截断恢复。

### P2

2. **P11-2 "SHOW BRANCHES 放行"三处声称失实（`sql/mod.rs:257-264`，探针 P11-6）**：一行修复（`kind==Show => 放行`）+ r9_2 用例清单补 SHOW/REOPEN（十轮 R10-6 原样）。**记账纪律**：修复未做却记 ✅，这是 R8-6（假修复）以来第三次"完成声称"失实——本轮虽无行为级危害，但 TASK.md 的证据列一旦失去可信度，整个 DoD 体系失效。
3. **P11-3 SQL 侧 R10-1/R10-3 零常驻回归（DoD #2 缺席）**：P0 修复的反退化网不存在；TASK.md R10-P0 证据列以 KV 侧测试充当 SQL 侧证据（且引用了不存在的测试名）。修法：把本轮探针 P11-1/2/3 转正为 `sql_semantics` 回归（COMMIT 后截断恢复 / 同快照计数与异序结束 / failed-COMMIT 注销），半小时级。

### P3

4. **P11-4 记账与文档失准合集**：TASK.md R10-P0 证据列测试名不存在；基线"208"实为 209；README:136 "7/7"、:173 "7 文件"、:125 状态段 2026-09-07 均陈旧（slt 实为 9）；kv.rs:1 引用的 SPEC 11 不存在；SPEC 04 §3 伪代码未反映 commit_tx 管线与 Q-9 新机制。
5. **P11-5 BEGIN 持 commit_mu 的延迟上界未声明**（`sql/mod.rs:431`、`kv.rs:306`）：BEGIN 最多等一个完整 checkpoint（含对象存储 PUT）。正确性代价可接受，但 G7 叙事下应注释声明并在 P2-3 基准中观测。

---

## 4. 收尾总评（十一轮收口）

### 4.1 十一轮轨迹一览

三轮起每轮必出新缺陷：R5→R6 keepalive 回归、R8-6 假修复、R9-1 注销缺失、R10 双装配/生命周期 P0——**本轮首次零新增 P0、零行为级假声称**，且上轮全部行为级问题（双 P0 + 三 P1）经九探针逐一验真关闭。"修复引入新缺陷"的模式（3/5 轮）在本轮中断。残余问题（use_branch 泄漏、SHOW 记账）均属"已登记欠账的利息"，非新机制缺陷。

### 4.2 G1–G7 最终达成度（终评）

| 目标 | 十轮 | **本轮终评** | 依据 |
|------|:----:|:----:|------|
| G1 双协议分离 | 🟡 | **✅（上调）** | wire 层质量 + 装配修复经进程级全组合验真（§2.4 表）；PG 优先的产品入口可用。**附条件**：Q-18 自动化防线未入 CI，防回弹靠人工——上调成立但防线债务在册 |
| G2 只写+版本化+O(1) 分支 | ✅ | **✅（维持）** | 写路径十一轮无新缺陷；本轮 commit_tx 失败路径注销闭合最后一个生命周期口 |
| G3 云原生 | 🟡 | **🟡（收窄）** | 内存有界恢复（探针 P11-1：COMMIT 后截断真实发生）；bind/exit、只读副本、fencing、metrics/readyz 齐备。残余：use_branch 泄漏路径（P11-1）、Q-12 优雅关闭十轮 ⬜（SIGTERM 语义缺失是 k8s 硬伤）、弹性调度的基准证据仍零 |
| G4 列存为 GPU 保留 | 🟡 | 🟡（不变） | M-4 连续十一轮零工作；Q-14 未修（try_ap_scan 本轮复核仍无 `sess.txn` 引用——AP 路径在事务语义之外） |
| G5 SQL 一等公民 | 🟡 | **🟡（接近收敛）** | 行式 TP + KV 的事务矩阵经本轮探针全绿；距 ✅ 差三项：Q-14（AP 路径）、SHOW 一行（P11-2）、Q-16/Q-5 等边角。SQL 主路径本身十一轮干净 |
| G6 压缩+量化衰退 | 🟡 | 🟡（不变） | M-3 连续十一轮零工作 |
| G7 性能绝对优先 | 🟡 | 🟡（改善） | clippy 门槛恢复、内存上界恢复为实质改善；欠账延续：R8-1 扫描物化基准对照第三年轮未补、P2-3 方法学未动、BEGIN×commit_mu 串行点未测量 |

### 4.3 TASK.md 全部未完项——优先级总排序

只列 ⬜/🔧 且非远期（P3-* 战略项不排序），按"对产品级目标的边际收益"排序：

| 序 | 项 | 现优先级 | 排序理由 |
|:--:|------|:----:|------|
| 1 | **Q-14** AP 路径读己写 + 冻结根 | P1 | 显式事务矩阵最后一个正确性洞；G5→✅ 与"列存可用性"的双重门槛 |
| 2 | **Q-18** 装配冒烟测试入 CI | P1 | 两次装配 P0 的结构性根因；本轮人工验证必须固化为代码，否则 G1 的 ✅ 随时可回弹 |
| 3 | **P1-7** 多线程 OCC 并发测试 | P1 | 十轮 ⬜、现成用例四条（含本轮 BEGIN×checkpoint 互斥的并发证明）；单线程编排验不尽 OCC |
| 4 | **Q-15** SPEC 03/04 同步 + **SPEC 11(KV)** + README/TASK.md 记账勘误 | P2→**升 P1** | 文档债已连续三轮产出评审发现（R9-8、R10 多处、本轮 P11-2/4）；q9 机制切换未记录是最急的一段 |
| 5 | **P1-8** 真 kill 崩溃恢复 | P1 | "drop(db) 模拟"从未被真 SIGKILL 检验；云原生叙事的底座证据 |
| 6 | **Q-12** 优雅关闭 + readyz 分离 | P2 | k8s 终止语义硬伤，十轮 ⬜；工作量小 |
| 7 | **Q-13** memtx 保留精细化 | P2 | 与 Q-9 联动定案；长事务下内存上界的最后优化空间 |
| 8 | **M-3 / M-4 / M-5** | P2 | G6/G4/G7 的证据域——长期零动是 G4/G6 无法上调的唯一原因 |
| 9 | **P2-3** 基准证据链 | P2 | 吞吐数字成立的前提；与 P2-6（热路径）合并做 |
| 10 | **P2-1** slt 语料扩展 | P2 | G5 的基线广度 |
| 11 | S-3 / Q-1 / Q-4 / Q-5 / Q-6 / Q-2 / Q-3 | P2 | 产品化外圈（资源上界/读内存上界/索引决策/SET/伪表/跨进程 DROP/启动校验） |
| 12 | Q-10（事务 DDL）/ Q-16 / Q-17 / Q-19 / Q-20 / P2-4..P2-8 / M-6..M-8 | P2/P3 | 记账性与小项，随批顺带 |
| 13 | P2'-2 / P2'-3 | ⬜ | 依赖 P3-1 决策（openraft 落地时机） |

记账更正两处：M-1/M-2 实际已交付（`b9161e6`：backup CLI + manifest format_version 守卫，本轮代码复核确认），M 表无状态列未体现，建议补 ✅；P1-10（time travel SQL）数据层就绪已久，属低成本高显示度项，可插队至第 9 位。

### 4.4 下一批最值得做的 3 件事

1. **「防线固化」批（约 1 天）**：SHOW BRANCHES 放行一行 + r9_2 补 SHOW/REOPEN 用例 + 把本轮探针 P11-1/2/3/7 转正为常驻回归（SQL 侧 R10 补课 + use_branch 泄漏断言）+ **Q-18 落地**：本轮进程级实验脚本化（spawn 真进程 → 逐端口协议判别 + bind 冲突 exit 码），进 CI。验收标准：每条新回归在人为回退对应修复时变红。这批做完，"修复自带其触达层的测试"才从口号变成资产。
2. **「事务矩阵收口」批（2–3 天）**：KV use_branch 事务语义（建议 25001 拒绝，与 SQL 对齐）+ **Q-14**（AP 路径接入层③写集 + 冻结根——`try_ap_scan` 复用 table_scan 的三层归并抽象即可，勿再写第二套）+ Q-16/Q-17 顺带。完成判据：显式事务 × checkpoint × 四条读路径（行扫/点查/AP/KV）矩阵在 SPEC 04 成表且全绿——这是 G5 上调 ✅ 的直接依据。
3. **「文档总清账」批（1–2 天）**：Q-15 全量（SPEC 03 §5 守卫清单含 SHOW 语义、SPEC 04 §3 重写为 commit_tx 管线 + Q-9 新机制"盲区兜底 + OCC 实裁"双层口径、tutorial 06 同步）+ 新增 SPEC 11（KV：isolation、use_branch、cas 线性化、RESP 命令面）+ README 状态段/计数刷新 + TASK.md 证据列勘误（R10-P0 测试名、M-1/M-2 补 ✅）+ AGENTS.md 落 Q-19 红线。文档债是近三轮评审发现的最大来源，一次性还清。

### 4.5 收敛判定（委托委托的明确回答）

**写路径 / 读路径（行式 + KV）/ 装配与生命周期的正确性防线：可以收敛。**
依据：① 本轮九探针（提交/回滚/失败提交/会话放弃 × SQL/KV × 同快照并发/异序结束 × 截断恢复/跳过）全部行为正确；② 首次零新增 P0，且"修复引入新缺陷"模式首次中断；③ 机制形态到位——引用计数注册表 + 全路径注销 + 互斥注册是十轮迭代的正确终点，不再有已知的设计级缺口。**条件**：§4.4 第 1 批（防线固化）必须先行——SHOW 一行、use_branch 封堵、SQL 侧回归转正三件收口后，"收敛"才从评审判断变成受测试保护的常态。

**尚未收敛、且不属于主干收口的域**：AP 列存路径（Q-14，事务语义之外）、真并发（P1-7）与真崩溃（P1-8）防线、产品化外圈（Q-12 优雅关闭、S-3 资源上界、M-7 SCRAM/TLS）。这些决定"产品级"三个字何时成立，但不再阻塞"引擎正确性"的收官。

---

## 5. 架构 / 选型 / 测试 / 文档诚信

### 5.1 架构（增量）

- **active_snaps 注册表最终形态**（`BTreeMap<u64,usize>` + 三路径注销 + commit_mu 互斥注册）结构正确，本轮无新洞；use_branch 是 SQL/KV 两套会话语义不一致的最后一处（SQL 拒绝 / KV 静默）——统一为"活跃事务内拒绝切换分支"即可消除整个类别。
- 装配层 HashMap 按名存取是对十轮"位置容器承载具名资源"错误的正确纠正；`main::serve` 仍是 250 行单体函数，Q-18 的"装配函数化"建议维持。
- BEGIN 纳入 commit_mu 临界区（正确性必需）使 BEGIN 与 checkpoint 串行——架构上可接受，建议在 P2-6/P2-3 中观测 BEGIN 尾延迟。

### 5.2 技术选型（增量）

- 本轮无新依赖、无选型争议。`BTreeMap` 计数、HashMap 具名监听器、`entry()` API 均为恰当选择。
- 十轮"对机制没想清楚就动刀"的批评本轮未再犯：注册表/装配两处改动都体现了对生命周期与锁序的完整推演（注释质量同步提升）。

### 5.3 测试机制

- 209 单测 + 9 slt + clippy 绿：既有防线密实且本轮门槛恢复。
- **缺口依旧集中在"修复所触达的层"**：SQL 侧 R10 修复零常驻回归（P11-3）——DoD 第 2 条在本轮修复批次中再次缺席（十轮评审建议的"DoD 第 4 条：修复必须自带其触达层的测试"仍未成文）。Q-18、P1-7、P1-8 三个"层对应"测试任务全部 ⬜。
- 值得肯定：`kv_txn_frozen_reads_and_registry_lifecycle` 是第一条"引擎状态断言"式回归（不只验行为、还验注册表状态）——这条路线应推广到 SQL 侧。

### 5.4 文档诚信（第十一轮抽查）

| 项 | 判定 |
|----|:----:|
| R10-1/2/3/4、P10-5 行为声称 | ✅ 五项全部经实跑/探针验真 |
| "SHOW BRANCHES 放行"（回应/commit/TASK.md 三处） | ❌ 代码未实现（探针 P11-6） |
| TASK.md R10-P0 证据列测试名 | ❌ 引用不存在的 `kv_txn_frozen_reads_and_decoded_values` |
| 基线 "208 通过" | 🟡 实为 209（新增测试后口径未更新） |
| Q-18/19/20 登记 | ✅ 如实（Q-19 未虚报完成） |
| M-1/M-2 交付未记账 | 🟡 代码在（backup CLI / format_version 守卫），TASK.md M 表无状态列 |

模式判断：**行为级声称全部为真（十轮以来首次），记账级声称仍有失实（SHOW、测试名、计数）。** "问题声称诚实、修复完成声称失实"的老模式已收敛为"修复完成声称基本可信、收尾记账粗糙"——方向向好，但 TASK.md 证据列的可信度需要一次勘误批（§4.4 第 3 件事）来恢复。

---

## 6. 建议行动顺序

1. **立即（1 天）**：P11-2 SHOW 放行 + P11-1 use_branch 封堵 + P11-3 SQL 侧回归转正（三件都是小时级）；clippy/test/slt 三合一本地门禁脚本落进 AGENTS.md（Q-19 红线一并写入）。
2. **随后（1 天）**：Q-18 装配冒烟测试脚本化入 CI；TASK.md/README 证据列勘误（P11-4）。
3. **本冲刺（2–3 天）**：Q-14 AP 路径 + Q-16/Q-17 顺带——事务矩阵收口，SPEC 04 成表。
4. **收尾（1–2 天）**：Q-15 全量 + SPEC 11 + README 刷新（§4.4 第 3 件事）。
5. **中期（入册不变，按 §4.3 序）**：P1-7 → P1-8 → Q-12 → M-3/M-4/M-5 → P2-3/P2-6 → P2-1 → 产品化外圈（S-3/Q-1/M-7）。

---

## 附：评审方法说明

**本机实跑**（HEAD = `7fe47dc`，评审前后工作树均干净，探针文件与全部临时产物已删除）：
- `cargo test --workspace`：**209 passed / 0 failed / 1 ignored**
- `cargo run -q -p slt -- run tests/slt/dendro`：**9/9**
- `cargo clippy --workspace --all-targets -- -D warnings`：**零输出（绿）**
- `cargo build --release`：成功（进程级实验用）

**探针实验**（临时测试 `crates/dendro-core/tests/probe_r11.rs`，九用例，运行记录后删除）：
- P11-1（✅R10-1）：`BEGIN;COMMIT` 后注册表空；此后 checkpoint covered_min 0→covered（截断恢复）。
- P11-2（✅R10-3/P10-5）：同 watermark 双会话计数 2 → s1 ROLLBACK 后 1 → 并发提交+checkpoint 截断跳过（covered_min 保持 0）、s2 冻结读完好 → s2 COMMIT 后空 → 截断恢复。
- P11-3（✅R10-1）：ROLLBACK / failed_txn-COMMIT 丢弃 / Session::drop 三路径注销皆空。
- P11-4（✅failed-COMMIT）：人为抬 covered_min → COMMIT 40001 且注册表空（commit_tx 失败路径注销）。
- P11-5（✅P10-5）：活跃事务期间他人 checkpoint → covered_min 不进；保留窗口内真冲突 40001（OCC 实裁）、非冲突正常提交。
- P11-6（❌声称）：事务内 `SHOW BRANCHES` → 25001（"放行"声称不成立）；REOPEN 仍 25001。
- P11-7（❌P11-1）：KV `begin()` 后 `use_branch("b2")` → main 注册表 `{0:1}`，会话 Drop 后仍残留（泄漏实锤）。
- P11-8（✅R10-4）：KV commit_tx 失败（40001）路径注销。
- P11-9（✅R10-4）：KV rollback/Drop 注销；KV+SQL 跨层同快照计数 2、异序结束归零。

**进程级实验**（真实二进制 + 临时数据目录，实例已终止、目录已删除；本机原有的一份 `dendro serve`（pid 990165）非本轮所启，未触碰）：
- 实验一（默认组合 kv=0，pg=18342/mysql=18306/metrics=18469）：pg 端口 SSLRequest→'N'、startup→'R' AuthenticationOk；mysql 端口服务器先发 `8.0.36-dendro` 握手；metrics `/readyz` 200、`/metrics` Prometheus 文本。banner 与实际一致。
- 实验二（四端口 +kv=18379）：pg 'R'、mysql 握手、kv RESP `+PONG`、metrics HTTP——四端口各自正确。
- 实验三（bind 冲突复验）：第三实例占用 pg 端口 → `dendro: pg listen ... failed: Address already in use (os error 98)`，exit=1，无 ready。

**逐行读过的源码**（行号以 `7fe47dc` 为准）：`engine.rs`（covered_min/active_snaps 定义 133-138、checkpoint_branch 844-851、截断+covered_min 条件块 1064-1078、auto-checkpoint 线程 1150-1165、Session/Drop/unregister 1194-1215、commit_tx 1246-1300 含 Q-9 检查 1261-1264）；`sql/mod.rs` 全文（branch_sql_kind 223-234、守卫 253-264、USE 分支 268-277、exec_batch 69-95、BEGIN 424-447、COMMIT 449-465、ROLLBACK 467-471、prepare 585-615、exec_prepared 805-820）；`kv.rs` 全文（use_branch 66-72、begin 300-316、commit 317-327、rollback 328-332、unregister 335-345、kv_entry 347-366、Drop 486-492）；`dendro-server/src/main.rs` 全文（CLI 端口定义 44-66、bind 循环 173-185、四处按名装配 187-246、banner 247-251）；`sql/scan.rs`（try_ap_scan 411-540，确认零 `sess.txn` 引用）；回归：`sql_semantics.rs` 全文、`kv_wire.rs::kv_txn_frozen_reads_and_registry_lifecycle`。

**读过的文档**：协议 README、第十轮评审与回应全文、TASK.md 全文、`spec/00` §1（G1–G7 度量）、`spec/04` §3-4、`spec/` 目录清单（确认无 SPEC 11）、AGENTS.md 红线段、README.md（状态/快速开始/测试段）、`manifest.rs` format_version 守卫（M-2 交付确认）。

**grep 核查**：`active_snaps`（8 处：定义/构造/判定/SQL 注册/SQL 注销/KV 注册/KV 注销/测试断言——无裸 `insert` 残留）；`covered_min`（5 处：定义/构造/条件 store/Q-9 检查）；`unregister_snapshot`（SQL 1 + KV 1 定义，调用点 7 处，use_branch 缺席）；`checkpoint_locked` 调用链（唯一入口 checkpoint_branch 持 commit_mu）；`commit_mu`（9 处，锁序一致）；slt runner `DbOptions::memory()`（Q-17 未做确认）；`git diff 796267c..7fe47dc -- sql/mod.rs`（确认守卫零改动）。
