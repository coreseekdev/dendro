# 架构评审（第十三轮·轻量复评）：Q-14 AP 路径事务语义 + P1-7 真并发 OCC + Q-15 文档清账——里程碑触发式聚焦验证

- 评审人：架构评审（外部）
- 日期：2026-09-08
- 对象：`git log c4abf34..HEAD` 全部两笔提交——`09f96f5`（Q-14 + P1-7）、`a615bca`（Q-15）；TASK.md 增量行
- 方法：按十二轮 §4.5 双触发制执行**半天级聚焦复评**——只审该批 diff + 新声称 + 新回归的"人为回退变红"抽查。**两枚探针（一回退、一双向），用后已删**，见文末方法说明。
- 基线（评审人本机实跑，HEAD = `a615bca`，评审后 `git status` 干净）：`./scripts/count-tests.sh` → **workspace 218 通过 / 0 失败 / 1 ignored；slt 9/9**；`cargo clippy --workspace --all-targets -- -D warnings` **零输出**；concurrent 三用例**连跑 5 次全绿**（每次 ~0.46s，无 flake）。

---

## 0. 总体判定（TL;DR）

**三个交付两真一空转**：Q-14 的**实现**经双向探针验证为正确（修复在位绿 / 还原后红，且回退后 `q14` 旧测试仍绿——见 P13-1）；P1-7 并发测试设计正确、断言确定、无竞态；Q-15 文档五点逐条对实现核实一致。基线数字与 `a615bca` 声称逐位一致，clippy 干净。

但**本轮抽查命中一次"人为回退不红"**：作为 G5→✅ 唯一门槛证据的 `q14_ap_path_reads_own_writes_and_frozen` 回归**从未接入列存（`set_columnar`），AP 路径在测试里短路返回 None，全程走行路径**——把整段 Q-14 修复还原后该测试照样通过。即：**Q-14 代码对，证据空**。新增发现 1×P1 + 4×P3，无 P0/P1 行为缺陷（P13-1 是证据级而非行为级）。

**G5 不上调 ✅**（依据见 §2）：最后一个正确性洞的实现已补齐且验证为真，但固化它的回归是空转的——G5 的✅必须等"证据真实化"，修复为一行 `set_columnar`（本评审探针 B 即成品测试，可直接转正）。

---

## 1. 委托点逐项 verdict

### 1.1 Q-14 AP 列存路径读己写 + 冻结根——**实现 ✅ / 回归 ✗（P13-1，P1）**

**实现正确性（逐层核对 + 探针双向验证）**：

- **三层归并顺序正确**：`scan.rs:497-547`——① CBF 行按 pk 入 BTreeMap → ② memtx overlay（`snapshot_rows(snapshot)`，Some 覆盖 / None 删除）→ ③ 会话显式事务写集**最后覆盖**（`scan.rs:526-545`，Put 插入 / Delete 移除，`tid != entry.id` 跳过）。与 `table_scan` 的单一抽象（`scan.rs:882-920`）同构，未写第二套机制——十二轮 §4.3 排序第 1 项的验收约束"复用 table_scan 三层归并抽象"literal 满足。
- **冻结根解析正确**：`scan.rs:431-441`——`sess.txn.explicit` 时以 `t.head_root` 走 `resolve_table_at`，与 `try_pk_pushdown`（`scan.rs:720-730`）、`table_scan`（`scan.rs:859-866`）三处同款；解析失败回退 `Ok(None)` → 行路径同口径报 undefined_table，无行为分叉。
- **触发条件无害**：`txn_has_writes` 仅在该表确有事务写时才进入归并（`scan.rs:502-507`），避免无写时白建 BTreeMap。
- **探针 B（双向，决定性）**：临时测试接入 `set_columnar(CbfColumnar{ row_group_rows: 4096 })` 复刻 q14 场景（1.2 万行 checkpoint 后显式事务 INSERT/UPDATE/DELETE + 非 pk 谓词查询 + 并发提交 + checkpoint + COMMIT）——**修复在位：全绿**（含 `WHERE v='upd'` 经 AP 归并可读、`WHERE v='v6'` 已删不可见、冻结读不被翻转）；**还原 `scan.rs` 至 `c4abf34`：红**（"事务内 UPDATE 经 AP 归并可读"期望 1 实得 0）——证明 Q-14 修复是承载性的、实现为真。探针已删除。
- 顺带核实的语义闭环：q14 的"冻结读 + COMMIT 成功"依赖 R9-1×P10-5 的联动——活跃快照使 checkpoint 跳过截断（`engine.rs:1075-1079`），`covered_min` 不推进，故 Q-9（`engine.rs:1262`）不误拒。测试实际覆盖了这条联动，是有价值的（在行路径上）。

**P13-1（P1）：q14 回归是空转测试——AP 路径从未被选中，"人为回退变红"抽查失败。**

- `sql_semantics.rs` 全文**无 `set_columnar` 调用**；`Database::open` 默认 `columnar()` 为 None（`engine.rs:439` `from_pointee(None)`，全仓仅 `ap.rs:17`/`gc.rs:53`/`s3_cloud.rs:69,116`/server 侧 4 处接入），于是 `try_ap_scan` 在 `scan.rs:429` 短路 `Ok(None)`，q14 的每一条 SELECT 都走 `table_scan` 行路径——而行路径的读己写/冻结读是 R7-3/R8-1 就有的。
- **探针 A（回退）**：`git checkout c4abf34 -- scan.rs` 后重跑 `q14_ap_path_reads_own_writes_and_frozen` → **照常通过**（24.9s）。该测试对 Q-14 修复的回退零灵敏度。
- 更刺的一处：测试内自称"**静态断言：确实走了 AP（否则本测试没测到目标路径）**"的代码块（`sql_semantics.rs:378-384`）实际是 `let o = ...; let _ = o;`——**没有任何断言**。注释意识到了这个风险，代码没有关闭它。
- 声称对照：commit message "Regression q14 drives a 12k-row checkpointed table through an explicit txn (insert/update/delete **via AP merge**...)"与 TASK.md Q-14 证据列均失实——恰是十二轮 §4.5 成文规范第 2 条（证据引用以 grep 命中为准）管不住的新变体：测试存在且 grep 可命中，但没测声称的东西。
- 严重度定 P1 而非 P0：行为无缺陷（实现正确），是**G5 门槛证据失效**；按十二轮 §4.5 例外触发条款（"行为级假声称"）本应插专项评审——本轮里程碑复评即是该评审。
- **修复 = 把探针 B 转正**：q14 测试补一行 `db.set_columnar(Arc::new(dendro_columnar::integrate::CbfColumnar { row_group_rows: 4096 }))`（+ `use std::sync::Arc;`），并删掉 378-384 的伪断言块或换成真断言。建议顺带把 `WHERE v=` 非 pk 谓词两条断言一并带入（现 q14 的点查断言全走 pk 下推路径，第三层归并只有 `count(*)` 在验）。

**与 table_scan 抽象一致性的两条备注**（记录在案，不立项）：① AP 第三层 Put 解码失败静默丢行（`if let Ok(r)`），table_scan 第三层原样入表延迟解码（解码失败报错）——坏行时两路径行为不同，但事务写集由引擎自编码，触发需引擎 bug；② `try_ap_scan` 的 `short_name` 只按 `.` 切分（`scan.rs:430`），显式事务内 `t@branch` 形态会解析失败→静默回退行路径（答案一致，仅丢 AP 加速），沿 `@` 切分的两处同名逻辑已存在。

### 1.2 P1-7 真并发 OCC——**✅（附 P13-5 表述精度）**

- **Barrier 同快照语义正确**：`concurrent.rs:39-51`——8 线程各自 `BEGIN`+`UPDATE`（同键）后在 `Barrier::new(8)` 汇合，放行前无任何提交，故 8 个事务必然持同一 watermark 快照。commit message 对"弱排序变体被有意拒绝"的解释（晚 BEGIN 合法拿新快照并赢）在 OCC 语义上是对的，屏障位置（UPDATE 后、COMMIT 前）选得准。
- **恰一赢家断言确定无竞态**：`concurrent.rs:53-71`——赢家数 == 1、败者 == 7 且各断言 `state == "40001"`、赢家值持久化、行数恒 1。异键用例（:77-94）真并行提交（各线程自行 `exec`），count == 8 断言排除丢更新；autocommit 用例（:97-125）"40001-or-win + 终值唯一 + 失败者均 40001"的不变式在两种交错下均成立，断言不依赖时序。scoped thread 内 panic 会随 scope 传播，失败不会静默。**5 次连跑全绿**，无 flake。
- **P13-5（P3）**：用例①的 8 个 COMMIT 实际由主线程 `join` 后**串行**发出（`concurrent.rs:53-57`：`h.join()` 阻塞至线程退出，再在主线程逐个 `exec("COMMIT")`）——"并发提交"的 TASK.md 表述（"8 线程真 Barrier 同快照**并发提交**"）强于事实：Barrier 保证的是同快照取写，提交阶段是串行化的。这不是缺陷——提交串行化正是"恰一赢家"确定性的来源之一（commit message 已如实解释设计取舍），且 commit_mu 的并行准入已由用例②③覆盖（各线程自行提交）——但建议 TASK.md/注释表述改为"同快照屏障 + 主线程串行提交"以免后续读者误判覆盖面。
- 遗留观测点（非本轮范围）：G7 在册的"BEGIN×commit_mu 尾延迟观测"未随 P1-7 交付（十二轮 §4.3 排序第 2 项的顺带项），保持挂账。

### 1.3 Q-15 SPEC 04 + tutorial 同步——**✅**

SPEC 04 `spec/04-memtx.md:72-89`（v1.1 语义增补五点）逐条对实现核实，全部一致：

| 增补声称 | 实现位置 | verdict |
|---|---|---|
| 冻结读：`Txn.head_root`，读路径冻结 / 写路径按当前 head | `memtx.rs:177`、`scan.rs:431-441/720-730/859-866`、`kv.rs:316,356` | ✅ |
| 活跃快照阻止截断（`active_snaps` 引用计数） | `engine.rs:1075-1079`（无活跃才 `truncate_all`） | ✅ |
| Q-9 跨 checkpoint 提交 40001；covered_min 只真截断推进 | `engine.rs:1262-1266`、`engine.rs:1073-1078` 注释与代码同口径 | ✅ |
| 读己写三路径同一抽象（table_scan/点查/AP） | `scan.rs:904-920 / 811-821 / 526-545` | ✅ |
| 事务内 USE/CHECKPOINT/CREATE\|DROP\|MERGE\|REOPEN → 25001，SHOW 放行 | `sql/mod.rs:253-268`，`BranchKind::Checkpoint`（:205）确实在守卫内 | ✅ |

tutorial 06 附录（`docs/tutorial/06-内存事务引擎OCC.md:128-135`）与 SPEC 04 同口径、面向用户语言改写准确（"重试即可"的客户端指引与 40001 语义一致）。

### 1.4 记账准确性——**三处偏差（P13-2/3/4）+ 委托口径勘误**

- **P13-2（P3）`09f96f5` commit message 基线数字失实**：声称 "Script-counted baseline: **215** passed"，其提交树实为 **218**（c4abf34 = 214 + q14 1 + concurrent 3；与 HEAD 本机脚本实跑 218 一致，`a615bca` 纯文档提交不增测试、其 218 正确）。215 恰为不含 concurrent.rs 三用例的中间态——疑为提交前未重跑脚本。DoD 第 5 条（"基线计数以 scripts/count-tests.sh 为准"）刚在 c4abf34 成文即出现违背实例。十二轮"基线数字逐位精确"的成就未能延续到本批。
- **P13-3（P3）新文件重新引入 blanket 豁免**：`concurrent.rs:1` `#![allow(clippy::all)]`——恰是十二轮 P12-3 刚从 `assembly.rs` 清除的反模式（十二轮 clippy 探针已证明该豁免无必要）。clippy 本轮零告警，豁免依旧无收益、徒增门槛漏洞。
- **P13-4（P3）README:173 残留 "SQL 基线（7 文件）"**：与 :136 M7 行 "9/9" 自相矛盾。7/7→9/9 的勘误批（cc8cd0d）漏改同文件第二处——P12-1"勘误批自身有漏项"的同型长尾第三次出现。
- **委托口径勘误（非研发问题）**：委托称本轮增量含"README slt 计数 7/7→9/9（M7 行）、kv.rs 幽灵 SPEC 11 标注（Q-15）"——`git log c4abf34..HEAD --name-only` 证实**两笔提交均未触碰 README.md 与 kv.rs**：M7 行改于 `cc8cd0d`、kv.rs 标注改于 `cc8cd0d` 的上一轮 `cc8cd0d` 之前（git log -L 确认为 cc8cd0d），均属十二轮已评审范围，非本批增量。
- TASK.md Q-8/Q-14/P1-7 划账行证据引用 grep 均可命中（测试名真实存在），仅 P13-5 的"并发提交"表述与 P13-1 的"AP merge"声称失实两处。

---

## 2. G5 达成度复评（委托核心问题：Q-14 是否解除 G5 最后一项）

**结论：不上调。G5 维持 🟡，残余性质从"实现缺失"收窄为"证据缺失"。**

| 维度 | 判定 | 依据 |
|---|---|---|
| 实现侧 | ✅ 本轮 closes | 探针 B 双向验证：AP 路径三层归并 + 冻结根在真列存接入下行为与行路径一致；回退即红，修复承载 |
| 证据侧 | ✗ | P13-1：作为 G5 门槛的 q14 回归未触发 AP 路径，回退不红；G5 的✅条件应是"防线随 CI 常驻"——现驻守 CI 的测试守的不是这条防线 |
| Q-16/Q-5 边角 | 未动 | 十二轮即注明"G5 距 ✅ 仅差 Q-14 与 Q-16/Q-5 边角"；Q-16/Q-5（P2）仍 open |

上调路径（分钟级，无新设计）：q14 接入 `set_columnar`（探针 B 转正，含非 pk 谓词断言）→ 回退探针自证变红 → G5 即可✅（Q-16/Q-5 若按十二轮口径视为"边角不阻门"）。在此之前，收敛判定的边界声明相应更新为：**AP 列存路径的正确性已实现但无 CI 防线**。

---

## 3. 下一步建议（按序，均为分钟到小时级）

1. **P13-1 修复（首位）**：q14 接入列存转正——探针 B 测试体即成品（`set_columnar` 一行 + import，建议带入 `WHERE v=` 两条非 pk 断言），删除 `sql_semantics.rs:378-384` 伪断言块；修复后自跑一次回退探针确认变红，把"人为回退变红"从评审抽查变成提交前自检动作。
2. **P13-2/3/4 顺手清**：concurrent.rs 删 blanket allow（P13-3）；README:173 改 9 文件（P13-4）；提交前重跑 `scripts/count-tests.sh` 再填数字（P13-2）——DoD 第 5 条的兑现动作。
3. **P13-5 表述校准**：TASK.md P1-7 行改为"同快照屏障 + 串行化提交（commit_mu 并行准入由②③覆盖）"。
4. 之后按十二轮 §4.3 排序继续：Q-12 优雅关闭（性价比最高插队项）→ P1-8 真崩溃。G5 转✅后，G1/G2/G5 三支柱全立，剩余 🟡（G3/G4/G6/G7）全部是第二阶段既定范围。

---

## 附：评审方法说明

**本机实跑**（HEAD = `a615bca`；评审起 working tree 仅临时改动两处，均已还原/删除，终态 `git status` 干净）：
- `./scripts/count-tests.sh`：**workspace 218 / 0 / 1 ignored；slt 9 文件全绿**（与 `a615bca` 声称逐位一致；与 `09f96f5` 声称的 215 不一致，见 P13-2）
- `cargo clippy --workspace --all-targets -- -D warnings`：**零输出**
- `cargo test -p dendro-core --test concurrent` ×5：**3 passed ×5**，各 ~0.46s，无 flake

**探针实验**（均已删除/还原）：
- **探针 A（回退）**：`git checkout c4abf34 -- crates/dendro-core/src/sql/scan.rs` → `cargo test --test sql_semantics q14_` → **PASS（24.9s）**——Q-14 修复整段还原后其回归仍绿，P13-1 实锤。随后 `git checkout HEAD --` 还原（diff 确认逐字节复原）。
- **探针 B（双向）**：临时 `crates/dendro-core/tests/probe_r13.rs`（q14 场景 + `set_columnar(CbfColumnar{row_group_rows:4096})` + 非 pk 谓词断言）：修复在位 → **PASS（8.9s）**；还原 `scan.rs` 至 c4abf34 → **FAIL**（"事务内 UPDATE 经 AP 归并可读"期望 1 实得 0，且 `WHERE v='v6'` 删行亦复活）——证明探针命中 AP 路径且修复承载。运行记录在案，文件已删除。

**逐行读过的代码**（行号以 HEAD 为准）：`scan.rs` 全文（重点 412-550 AP 路径、696-746 pk 下推、831-934 table_scan）；`engine.rs` 354-363（columnar ArcSwap 默认 None）、837-1081（checkpoint 物化与截断守卫）、1187-1299（Session/commit_tx）；`memtx.rs` 169-210（Txn/validate_only）；`sql/mod.rs` 190-300（BranchKind 与守卫）；`tests/concurrent.rs` 全文；`tests/sql_semantics.rs` 360-418（q14）；`tests/ap.rs`（set_columnar 惯用法）；`scripts/count-tests.sh`。

**核对过的记账**：TASK.md Q-8/Q-14/P1-7 三行；README:136/:173；kv.rs:1 标注来源（git log -L 确认为 cc8cd0d，非本批）；`git log c4abf34..HEAD --name-only` 全量清单；全仓 `set_columnar` 调用点 grep（7 处，均不在 sql_semantics.rs）；spec/04-memtx.md 与 tutorial 06 全量 diff。

**读过的文档**：协议 README、第十二轮评审全文、TASK.md 相关段。
