# 回应：架构评审（第十六轮里程碑复评）

- 回应人：dendro 开发者
- 日期：2026-09-08
- 对象：`2026-09-08-架构评审-第十六轮-里程碑复评.md`
- 门槛（脚本计数）：`scripts/count-tests.sh` → workspace **220 通过 / 0 失败** / 1 ignored；slt 9/9；clippy `-D warnings` 零输出

---

## 0. 总体态度

R16-1 完全成立——Q-12 回归用了 Group 持久级（exec 返回前缓冲必空），
"NoWait 缓冲尾"从未执行，与 R13-1 空转测试同类（"镜像方向零覆盖"第二次
应验）。已改 NoWait 持久级 + 变异自检（禁用 flush → 红，还原 → 绿）。
评审确认实现本身正确（双向探针验真），本轮补齐证据。

## 1. 修复

- **R16-1**：回归改 NoWait 持久级 + 变异自检记录。
- **P1-8 真崩溃（评审下一批首位）同步交付**：
  `dendro-server/tests/crash.rs`——spawn 真实 serve 进程，经 **PG 线协议**
  （自写极简客户端：Startup/Query/DataRow 解析）写入 5 行（3 行
  checkpoint + 2 行仅 WAL）→ **SIGKILL** 子进程 → 重启 → 5 行全部可见
  → 崩溃后可继续写。G3 的真崩溃缺口关闭。
- R16-2/R16-3：登记为 Q-12b（停监听 + live/readiness 分离余项）/
  Q-12c（shutdown join checkpoint 线程）。

## 2. 基线（脚本计数）

`scripts/count-tests.sh`：**220 通过 / 0 失败 / 1 ignored**；slt 9/9；
clippy `-D warnings` 零输出。
