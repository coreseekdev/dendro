# 回应：架构评审（第十二轮）

- 回应人：dendro 开发者
- 日期：2026-09-08
- 对象：`2026-09-08-架构评审-第十二轮.md`
- 门槛：`cargo test --workspace` **214** 通过 / 0 失败；slt 9/9；clippy `-D warnings` 零输出

---

## 0. 收敛判定接受

**主干正确性防线收敛——终局确认接受**。连续两轮零新增 P0/P1、基线数字
逐位精确（本轮修正后）、G1→✅ 条件满足。"收敛不是宣布出来的，是这条轨迹
走出来的"——评审机制十二轮的值已全额兑现。

## 1. 六个 P3 全部清偿

| 项 | 处置 |
|----|------|
| P12-1 TASK.md 幽灵测试名 + 聚合脚本承诺无载体 | 测试名更正为真实路径；**脚本落地**：`scripts/count-tests.sh`（聚合 test/result 计数，回应基线从此脚本化） |
| P12-2 守卫注释引用错误测试名 | 更正为 `r10_show_branches_allowed_and_snapshot_lifecycle` |
| P12-3 assembly.rs blanket allow | 删除（clippy 探针证实无必要——防守性豁免即门槛漏洞，接受） |
| P12-4 readiness 判定弱于注释 + 空响应越界 | 轮询真判 `HTTP/1.1 200`；空响应先断言非空再取 [0] |
| P12-5 默认组合未自动化 | 新增 `assembly_default_combo_three_ports`（kv=0 三端口，README 快速开始路径） |
| P12-6 两条探针转正 | REOPEN-in-txn 入 r9_2 用例清单（+CREATE BRANCH bx 前置）；use_branch 成功路径隔离入 kv_wire（顺带勘误：use_branch 不自动建分支——自动建语义在 kv_resp 的 BRANCH 命令） |

## 2. 评审机制收尾建议接受

停止固定轮次，转 **"里程碑聚焦复评 + 例外专项"双触发**：
- 里程碑复评：Q-14 + P1-7 完成后的轻量复评（第 13 轮预约）；
- 例外专项：任何 P0/P1 级发现随时发起。
DoD 第 4 条（证据引用以 grep 命中为准）、第 5 条（基线计数脚本化）、
第 6 条（replace 必须 assert——本轮已实践）一并入册。

## 3. 基线（脚本化计数）

`scripts/count-tests.sh`：**214 通过 / 0 失败**；slt 9/9；clippy `-D warnings`
零输出。本轮新增：assembly 默认组合用例 ×1、REOPEN 用例转正 ×1、
use_branch 隔离 ×1、装配 readiness 判定强化。
