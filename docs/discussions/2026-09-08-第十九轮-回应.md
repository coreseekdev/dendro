# 回应：架构评审（第十九轮轻量复评·未完成转自检）

- 回应人：dendro 开发者
- 日期：2026-09-08
- 对象：第十九轮复评中断（网络故障）前的部分发现 + 研发自查补完
- 门槛（脚本计数）：`scripts/count-tests.sh` → workspace **226 通过 / 0 失败** / 1 ignored；slt 9/9；clippy `-D warnings` 零输出

---

## 0. 总体态度

中断的评审在部分输出中已实证三条失实，全部接受并补清（评审中断未完成
的验证由研发自查补完，逐项附实跑证据）。

## 1. 验证与修复

### ① DoD 第 7 条未落文 → ✅ 已落文
interrupted 输出实证：63bdc15 与 2c28fe7 两次声称"落文"均未发生——
grep 到的"第 7 条"字样全部来自**队列表格的散文引用**，DoD 段本身只有
1-3 条。本轮真正写入 `docs/discussions/README.md` 完成定义（第 4-7 条
四条一次性补齐：grep 命中/脚本计数/replace assert/变异自检）。

### ② R18-8 记账未清偿 → ✅ 已补清
TASK.md Q-1 行拆分（Q-1a ✅ / Q-1b ✅ / Q-1c 未做）与 M-3 行勘误
（Raw 1.06x / RleDict 0.97x / Zstd 3.91x，R18-2 勘误后口径）此前两次
脚本 replace 均静默失败。本轮以 assert+regex 双保险落地。

### ③ Branch::stopping 死字段 → ✅ 已删除
`Branch.stopping: AtomicBool`（engine.rs:135）全仓无读取点——Q-12b
的正确读点在 `Database.stopping`（:357，经 `is_stopping()` 消费）。
Branch 级字段是脚本编辑的残留，已删除。

### ④ main.rs 信号线程注释失实 → ✅ 已更正
"随 join 阻塞至进程退出"改为"Signals::new 失败时线程立即结束：进程随
监听器 join 正常退出，无优雅刷新"。

### ⑤ R18-1 变异自检 → ✅ 自查复跑
评审中断前的变异（恢复 WHERE 前截断 → limit_with_where 红 0 vs 5）
已由研发独立复跑：还原后 3 连绿。

## 2. 基线（脚本计数）

`scripts/count-tests.sh`：**226 通过 / 0 失败 / 1 ignored**；slt 9/9；
clippy `-D warnings` 零输出。
