# 回应：架构评审（第十三轮轻量复评）

- 回应人：dendro 开发者
- 日期：2026-09-08
- 对象：`2026-09-08-架构评审-第十三轮-轻量复评.md`
- 门槛（脚本计数）：`scripts/count-tests.sh` → workspace **218 通过 / 0 失败** / 1 ignored；slt 9/9；clippy `-D warnings` 零输出

---

## 0. 总体态度

R13-1 完全成立且是本轮最重要的一课：**q14 是空转测试**——放在 core 侧
无法 set_columnar，AP 路径短路后全程走行路径；"静态断言确实走了 AP"实际
是 `let _ = o;` 零断言。评审探针 A（还原 Q-14 修复仍绿）与探针 B（接入
列存后修复生效）定位精准。这是"证据缺失"而非"实现缺失"——但按 DoD，
无法自证的修复等同未交付，G5 维持 🟡 的判定正确。

## 1. 修复

- **q14 真实化**：移至 `dendro-server/tests/ap_txn.rs`（列存依赖可用）——
  `set_columnar` 接线 + **catalog col_rows ≥ 1 万的门断言**（AP 门不满足
  即失败，不再可能静默走行路径）+ 完整事务矩阵（INSERT/UPDATE/DELETE
  经 AP 归并、冻结读、COMMIT 后新快照）。原 core 侧空转版已删除。
- **P13-3**：concurrent.rs 的 blanket allow 删除（P12-3 反模式回归——
  教训：新文件复制模板时把豁免也复制了）。
- **P13-5**：COMMIT 移入线程（barrier 后真并发提交），不变式不变；
  "并发提交"表述自此与实现一致。
- **P13-4**：README "SQL 基线（7 文件）" → 9 文件。
- **P13-2**：commit message 计数失准勘误记录在案（已成文 DoD 第 5 条为
  永久防线；历史提交不改）。

## 2. G5

AP 事务语义真实化 + 回归入 CI 后，G5 的最后一项残余（Q-14）清偿——
G5 上调 ✅ 的条件已具备，请复核确认。

## 3. 基线（脚本计数）

`scripts/count-tests.sh`：**219 通过 / 0 失败 / 1 ignored**；slt 9/9；
clippy `-D warnings` 零输出。

## 4. 追加交付（第十三轮复评意见触发 + 第二阶段②）

- **Q-12 优雅关闭**落地：SIGTERM/SIGINT → `Database::shutdown`（停
  checkpoint 线程 + 全驻留分支 WAL `close_graceful`）→ exit 0；
  毒化分支跳过上传（错误语义保留）。G3 的 k8s 终止语义硬伤消除。
  回归：`wal_corruption::close_graceful_flushes_no_wait_tail`。

## 5. 第二阶段②（Q-1b 游标 + Q-12b 停止排流 + M-3 压缩曲线）

- **Q-1b 游标**：DECLARE name CURSOR [FOR] query / FETCH n|ALL|NEXT
  [FROM] name / CLOSE name——INSENSITIVE（DECLARE 时物化）/ READ ONLY /
  会话级 v1 口径；34000 未声明/已关闭。回归
  `sql_semantics::q1b_cursor_declare_fetch_close` +
  `q1b_cursor_is_insensitive_snapshot`。GRAMMAR 三行转 ✅。
- **Q-12b 停止排流**：`Database::begin_stopping`/`is_stopping` +
  /readyz 503 "shutting down"（信号线程先置位再 flush）。
  回归 `metrics_endpoint::readyz_reports_503_when_stopping`。
- **M-3 首批**：`dendro bench-compress`（`bench::compression_curve`）——
  Raw/RleDict/Zstd 大小比 + 编/解吞吐 JSON 落盘（BitPack 对文本列不适用
  自动跳过）。实测 2 万行：Raw 2.1x / RleDict 1.9x / **Zstd 7.8x**
  (enc 0.49 MB/s, dec 5.3 MB/s)。Q 曲线数据可脚本采集。
