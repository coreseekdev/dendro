# 回应：架构评审（第九轮）

- 回应人：dendro 开发者
- 日期：2026-09-08
- 对象：`2026-09-08-架构评审-第九轮.md`
- 门槛：`cargo test --workspace` 208 通过 / 0 失败；slt 9/9；clippy `-D warnings` 零输出

---

## 0. 总体态度

R9-1（P0）成立且比第七轮 R7-1 更深一层：R7-1 修的是归并逻辑，R9-1 暴露的
是归并逻辑的**输入本身会被 checkpoint 截断**——冻结读的"读"字没有任何
机制兜底。修复采用评审建议的"truncate 按最老活跃快照"（v1 保守口径：
存在活跃显式事务即本轮不截断，按版本精细化保留列 Q-13）。Q-9 的写侧
40001 维持不变——读保留与写冲突检测是两回事，已在代码注释与 TASK 口径
中分开表述。

## 1. 修复

- **R9-1（P0）**：`Branch.active_snaps` 注册表（BEGIN 注册、COMMIT/
  ROLLBACK/会话 Drop 注销）；checkpoint 截断跳过条件 = 存在活跃快照。
  回归 `r9_1_frozen_reads_survive_checkpoint`（BEGIN 前提交的行在并发
  提交 + checkpoint 后事务内恒可见、COMMIT 后新快照可见新行）。
- **R9-2（P1）**：`exec_branch_statement` 入口拒绝事务内 CHECKPOINT/
  CREATE/DROP/MERGE/REOPEN（25001）——CHECKPOINT 会在提交时刻自伤
  （撞 Q-9），其余为不可回滚 catalog 写（Q-10 事务化前保守口径）。
  回归逐事务验证四条语句（PG aborted 语义：首错后 25P02 接管，用
  ROLLBACK 分隔用例）。
- **R9-4（P1）**：**全部监听器前置 bind**——装配处先绑四个端口（任一
  冲突 eprintln + exit(1)，不打印 ready），再 spawn；pgwire/kv_resp 增
  `serve_listener`、mywire 增 `serve_listener`/`bind_on`。端到端验证：
  占用 pg 端口的第二实例 exit=1 且无 ready 输出。上轮"join 修复"确实
  无效（bind 在线程内 + metrics 永不返回），本轮为真修复。
- **R9-5（P2）**：KV 双洞——GET 事务写集路径解码取值列（此前返回整行
  编码）；SCAN 增加事务写集层（最后覆盖：树/overlay 会把事务内删除的
  键写回——测试当场抓到该层级顺序错误）。回归
  `kv_wire::kv_txn_reads_own_writes_and_decoded_values`。

## 2. 入册

- **Q-13（P2）** memtx 保留精细化（活跃快照存在 → 全量保留的内存权衡）；
- **Q-14（P1）** AP 列存路径读自己的写 + 冻结根（R9-3，本轮未完成——
  需 AP 扫描的 overlay 合并与冻结 catalog 解析，列入下一批首位）；
- **Q-15（P2）** SPEC 03/04 + tutorial 同步；
- **Q-16（P3）** 事务内重复 INSERT 23505；
- **Q-17（P3）** slt runner 显式 checkpoint_interval_s=0。

## 3. 基线（本地实跑）

208 通过 / 0 失败 / 1 ignored；slt 9/9；clippy `-D warnings` 零输出。
新增回归：r9_1、r9_2、kv_txn（3 文件/用例组）。
