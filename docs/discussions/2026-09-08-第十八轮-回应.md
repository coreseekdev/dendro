# 回应：架构评审（第十八轮轻量复评）

- 回应人：dendro 开发者
- 日期：2026-09-08
- 对象：`2026-09-08-架构评审-第十八轮-轻量复评.md`
- 门槛（脚本计数）：`scripts/count-tests.sh` → workspace **226 通过 / 0 失败** / 1 ignored；slt 9/9；clippy `-D warnings` 零输出

---

## 0. 总体态度

R18-1（P1）完全成立——WHERE 非空时 LIMIT 下推就是"先截断后过滤"，探针
实证的静默漏行是最严重的用户可见缺陷类别。修复为评审建议的一行口径
（selection 非空禁用下推）+ WHERE+LIMIT 回归。R18-7 的"声称已修实未修"
第三次接受，本轮以 grep 落实（DoD 第 7 条已在 discussions/README.md，
P1-8 队列行已划线）。

## 1. 修复

- **R18-1（P1）**：LIMIT 下推前置条件加 `select.selection.is_none()`；
  WHERE+ORDER BY 组合不变（ORDER BY 仍全量排序）。回归
  `ap_txn::limit_with_where_does_not_pushdown`（1000 行表 WHERE id>=900
  LIMIT 5 → 恰 5 行，修复前 0 行）。
- **R18-3**：DECLARE query 提取改 token 字节定位（连续空白不错位）。
  回归 `sql_semantics::r18_3_declare_with_multiple_spaces`。
- **R18-2/R18-6**：raw_bytes 去除 `*2` 双计（arrow 59.3 已求和）；单位
  更正为 Mrows/s（字段 `enc_mrows_s`/`dec_mrows_s`）；"BitPack 自动跳过"
  表述更正（实为对文本列返回 not-applicable 由调用方换 codec）。勘误后
  实测：Raw 1.06x / RleDict 0.97x / Zstd 3.91x。
- **R18-7**：DoD 第 7 条已确认落文 discussions/README.md；TASK P1-8 行
  划线；幽灵测试名 `kv_txn_frozen_reads_and_decoded_values` 更正。

## 2. 登记转化

- R18-5（事务内游标 ROLLBACK 后仍可 FETCH）：口径文档化——游标为
  INSENSITIVE 物化，与事务解耦是有意行为（SPEC 04 附录已含冻结读口径）；
  写侧隔离不受影响（游标只读）。
- R18-9：Branch::stopping 死字段（不存在的字段名）与排流窗口毫秒级
  观察登记为低优先观察项。

## 3. 基线（脚本计数）

`scripts/count-tests.sh`：**226 通过 / 0 失败 / 1 ignored**；slt 9/9；
clippy `-D warnings` 零输出。新增回归 3：WHERE+LIMIT（R18-1）、DECLARE
多空格（R18-3）、以及 ap_txn 原有用例自动获得非 pk 断言保护。
