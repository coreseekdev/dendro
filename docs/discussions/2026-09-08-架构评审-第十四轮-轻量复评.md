# 架构评审（第十四轮·轻量复评）：R13-1（q14 空转测试真实化）验证——门真、路真、核心归并层仍无灵敏度

- 评审人：架构评审（外部）
- 日期：2026-09-08
- 对象：`git log a615bca..HEAD` 单笔提交 `ed1cf72`（R13-1 修复 + P13-3/4/5 清账）
- 方法：按十二轮 §4.5 双触发制执行里程碑聚焦复评，验收判据即委托 prescribed 的**变异测试**："注释掉 scan.rs 中 AP 路径的 txn 写集层 → ap_txn 必须红"。共**两枚变异 + 一枚探针，用后已全部还原**（终态 `git status` 干净，HEAD = `ed1cf72`），见文末方法说明。
- 基线（评审人本机实跑）：`./scripts/count-tests.sh` → **workspace 218 / 0 / 1 ignored；slt 9/9**；`cargo test --workspace` 全绿；`cargo clippy --workspace --all-targets -- -D warnings` **零输出**；concurrent 三用例**连跑 5 次全绿**（各 ~0.46s，无 flake）。与 `ed1cf72` commit message 声称逐位一致——**DoD 第 5 条本轮兑现**（P13-2 类失准未复发）。

---

## 0. 总体判定（TL;DR）

**R13-1 修复半真**：q14 确已移至 server 侧并接线列存——`set_columnar` 在位（`ap_txn.rs:18`）、col_rows ≥ 1 万门断言真实（`ap_txn.rs:35`，与 `scan.rs:442` 的 AP 门同口径）、测试确实走进 AP 路径（变异 2：禁用冻结根解析 → **红**）。P13-3/4/5 三项清账全部验真。

但**委托验收判据失败**：变异 1（禁用 `scan.rs:526-545` 的 AP 事务写集归并层③）→ `ap_txn` **照样全绿**。原因经静态+动态双重定位：测试的全部写可见性点断言（`WHERE id = X`）在 `table_scan_opt`（`scan.rs:398-408`）里被 **pk 下推（`scan.rs:400`）先行截走**，走的是行路径的 R8-1 读己写（`scan.rs:811-821`）；唯一到达 AP 归并的 `count(*)` 断言又因事务内 +1 插 / −1 删恰好抵消而对层③不敏感。即：**Q-14 的"AP 路径读自己的写"这半边，CI 里依然没有一条会因它回退而变红的断言**——与第十三轮同病，只是病灶从"整条路径短路"缩小为"核心归并层无灵敏度"。探针 C 实证：补上两条非 pk 谓词断言后，变异 1 立即变红——修复配方已验证，四行以内。

**G5 不上调 ✅**（维持 🟡）：证据门槛（"人为回退变红"）未达标；距 ✅ 只差两条已写好、已双向验证的断言。

新发现：**1×P1 + 1×P3**，无行为级缺陷。

---

## 1. 委托点逐项 verdict

### 1.1 (a) ap_txn.rs 真实性——**部分 ✅：门真、路真、层③不设防（R14-1，P1）**

**做对的部分**：

- **列存接线真实**：`ap_txn.rs:18` `db.set_columnar(Arc::new(CbfColumnar { row_group_rows: 1_048_576 }))`——第十三轮 P13-1 的根因（core 侧无法 set_columnar）已结构性消除；文件头注释（`ap_txn.rs:4-7`）把教训写成了警示。
- **门断言真实且同口径**：`ap_txn.rs:26-36` 经 `catalog_lookup` 断言 `entry.col_rows >= 10_000`，与 AP 门的 `entry.col_rows < 10_000` 短路条件（`scan.rs:442`）同口径——该门**永久性封死**"AP 短路静默走行路径"这类失败模式（物化不达标时测试自己先红，而非空转）。
- **冻结根半边受保护**：变异 2（`scan.rs:433-437` 冻结根解析改为恒 `None`）→ 测试**红**于"冻结读被并发 checkpoint 翻转"（期望 12000 实得 12001）——Q-14 的冻结根半边有真实的回退灵敏度。
- 事务矩阵的场景设计（INSERT/UPDATE/DELETE + 冻结读 + COMMIT 后新快照 + 并发 checkpoint）本身完备，旧空转版已从 `sql_semantics.rs` 删除（grep 无残留）。

**R14-1（P1）：AP 事务写集归并层③对回归零灵敏度——委托验收判据失败。**

- **变异 1**：将 `scan.rs:526-545`（层③：会话显式事务写最后覆盖）整体禁用 → `cargo test -p dendro-server --test ap_txn` → **PASS（9.04s）**。整段"读自己的写经 AP 归并"的实现被废掉，测试纹丝不动。
- **机理解剖**（静态路由 + 探针双向证实）：
  1. `table_scan_opt`（`scan.rs:398-408`）路径优先级 = pk 下推（:400）→ AP（:405）→ 行扫描（:408）。测试的四条写可见性断言全是 `WHERE id = X` 点查形态（`ap_txn.rs:50,51,52-56`）→ 全部被 `try_pk_pushdown` 截走，走行路径的 R8-1 读己写（`scan.rs:811-821`）——**行路径替 AP 路径付了账**。
  2. 唯一到达 AP 的 `SELECT count(*) FROM big`（无谓词）期望值 12000 恰为 +1 插 / −1 删的抵消项——层③在位与否结果相同。冻结读（:63）与 COMMIT 后（:65-66）两条 count 也不经过层③。
- **探针 C（双向，决定性）**：在 `ap_txn.rs` 临时补两条**非 pk 谓词**断言（`WHERE v = 'upd'` 期望 1、`WHERE v = 'v6'` 期望 0——恰是第十三轮建议"顺带带入"而被漏掉的两条）：修复在位 → **绿（9.19s）**；重放变异 1 → **红（8.81s，"txn UPDATE 经 AP 归并可读"期望 1 实得 0）**。证实病灶与配方。
- **记账口径**：TASK.md R13-1 行"事务矩阵**全链**"、回应"INSERT/UPDATE/DELETE **经 AP 归并**"、commit message "insert/update/delete **through the AP merge**"——三处均强于证据：经 AP 归并被验证的只有抵消型 count；INSERT/UPDATE/DELETE 的可见性断言实际全走行路径。这是连续第三轮"声称强于证据"落在同一条测试上（P13-1 的直接后继），只是从 P1"整体空转"收窄为 P1"核心层无灵敏度"。
- 严重度维持 P1 的理由与十三轮同口径：行为无缺陷（层③实现本身经探针 C 在位侧验证为正确），但它是 **G5 门槛的验收判据失效**。

**修复（分钟级，配方已验证）**：在 `ap_txn.rs:56` 后补探针 C 的两条断言（原样）：

```rust
assert_eq!(q(&mut s, "SELECT count(*) FROM big WHERE v = 'upd'"), "1", "事务内 UPDATE 经 AP 归并可读");
assert_eq!(q(&mut s, "SELECT count(*) FROM big WHERE v = 'v6'"), "0", "事务内 DELETE 经 AP 归并不可见");
```

并按十三轮 §3.1 的建议，把"注释层③ → 必须红"固化为提交前自检动作（一行注释或脚本步骤均可），否则同一病灶有第四轮复发的现实风险。

### 1.2 (b) P13-5 并发提交真实化——**✅**

- COMMIT 已移入工作线程：`concurrent.rs:48-50`——`barrier.wait()`（同快照全体就绪）之后各线程**自行** `s.exec("COMMIT")`；`join` 循环（:59-62）只收集结果，不再代发提交。与上轮"join 后主线程串行 COMMIT"（十三轮 §1.2 P13-5）的结构对比成立。
- Barrier 语义未被移动破坏：放行前 8 线程均完成 BEGIN+UPDATE、零提交，同快照前提保持；"恰一赢家 + 7×40001 + 赢家值持久 + 行数 1"的不变式原样（:63-70），确定性由 commit_mu 串行化验证保证，不依赖时序。
- **5 次连跑全绿**（各 ~0.45-0.46s），无 flake。TASK.md R13-5 行表述自此与实现一致。

### 1.3 (c) P13-3 / P13-4 验真——**双双 ✅（但见 R14-2）**

- **P13-3 ✅**：`concurrent.rs` 首行 blanket allow 已删（`git show ed1cf72` 确认 `-#![allow(clippy::all)]`）；clippy `-D warnings` 本轮零输出，豁免依旧无收益。
- **P13-4 ✅**：README 测试段 "SQL 基线（9 文件）"，与 M7 行 9/9 一致，同文件无第二处残留。

**R14-2（P3）：同一提交一手拆豁免、一手新建豁免——反模式第三次出现。**

- 新文件 `ap_txn.rs:1` 以 `#![allow(clippy::all)]` 开头——与 concurrent.rs 被删的是同一个东西，且在同一笔提交里。研发在回应里对 P13-3 的归因原话是"**新文件复制模板时把豁免也复制了**"——本笔的新文件精确复演了这个归因。
- 需要指出系统性事实：全仓 grep 显示 blanket `#![allow(clippy::all)]` 仍遍布约 15 个测试/入口文件（`sql_semantics.rs`、`ap.rs`、`e2e.rs`、`kv.rs`、server 侧全部 8 个测试、`main.rs`/`lib.rs` 等）。逐文件打地鼠（P12-3 → P13-3 → R14-2）已三轮，建议**要么一次性清扫全部测试文件并跑一次 clippy 探针定案，要么在 AGENTS.md 成文"测试文件豁免属已知接受"**——二选一，终止这个循环。

### 1.4 (d) 其余记账——**✅**

- commit message 基线 "218 passed / 0 failed; slt 9/9; clippy clean" 与本机实跑逐位一致（P13-2 未复发，DoD 第 5 条起效）。
- `docs/discussions/README.md` 队列已更新（十三轮评审+回应入账，十二轮待回应注记如实）。
- 委托口径勘误（非研发问题）：委托称回应含"P13-2：commit message 计数失准勘误记录在案"为修复项——实为记录性说明，无代码动作，本评审按记录验读（历史提交确未改）。

---

## 2. G5 达成度判定（委托核心问题：研发声称"上调条件已具备，请复核确认"）

**结论：不上调。G5 维持 🟡。研发的声称不成立——条件差两条断言。**

| 门槛要件 | 判定 | 依据 |
|---|---|---|
| Q-14 实现（AP 读己写 + 冻结根） | ✅（十三轮探针 B + 本轮探针 C 在位侧双证） | 层③与冻结根行为正确；探针 C 在位绿/变异红证明实现承载 |
| Q-14 回归（CI 防线随常驻） | ✗ | 委托验收判据（变异 1 → 必须红）失败：层③回退测试照绿；冻结根半边虽已设防（变异 2 红），但"读自己的写"——Q-14 的标题语义——仍无灵敏度 |
| Q-16/Q-5 边角是否阻门 | **不阻**（复核确认） | Q-16（事务内重复 INSERT 静默覆盖，P3）是引擎级唯一性检查缺失，行/AP 两路径共享同一写集，非 AP 分叉，不属"AP 事务语义与行路径一致"范畴；Q-5（SET 假 OK，P2）是协议工效项。二者按十二轮 §4.2 口径属"边角"，**确认不拦 G5**，继续留在 open 台账即可 |

**上调路径（分钟级，无新设计，全部要素已验证）**：ap_txn.rs 补探针 C 两条断言 → 自跑一次变异 1 确认变红 → 还原 → G5 即 ✅（Q-16/Q-5 按上表口径不阻门）。上调后，边界声明更新为"SQL 一等公民的 AP 事务一致性有 CI 防线；Q-16/Q-5 为非阻门边角、在册清偿"。

在此之前，G5 的残余性质从十三轮的"证据缺失"（测试全空转）收窄为"证据半缺"（路径真、门真、冻结根真，唯核心归并层不设防）。

---

## 3. 下一步建议（按序）

1. **R14-1 修复（首位，分钟级）**：ap_txn.rs 补两条非 pk 谓词断言（§1.1 配方原样）+ 把"注释层③必须红"固化为提交前自检；随后 G5 → ✅。
2. **R14-2 收口（小时级）**：一次性清扫全部测试文件的 blanket allow 并以 clippy 探针定案，或成文接受——终止三轮循环。
3. 之后按十二轮 §4.3 排序继续：Q-12 优雅关闭 → P1-8 真崩溃。G5 转✅后 G1/G2/G5 三支柱全立。

---

## 附：评审方法说明

**本机实跑**（HEAD = `ed1cf72`；评审起 working tree 仅临时改动，终态 `git status` 干净、无 MUTATION/PROBE 残留、`git diff` 空）：

- `./scripts/count-tests.sh`：**workspace 218 / 0 / 1 ignored；slt 9 文件全绿**（与 commit message 逐位一致）
- `cargo test --workspace`：全绿（ap_txn 1 passed，8.89s）
- `cargo clippy --workspace --all-targets -- -D warnings`：**零输出**
- `cargo test -p dendro-core --test concurrent` ×5：**3 passed ×5**（0.45-0.46s），无 flake

**变异与探针实验**（均已还原，还原后 diff 确认逐字节复原）：

| 编号 | 操作 | 结果 |
|---|---|---|
| 变异 1 | 禁用 `scan.rs:526-545` 层③（`if t.explicit` → `if false && t.explicit`） | `ap_txn` **PASS 9.04s**——R14-1 实锤 |
| 变异 2 | 禁用 `scan.rs:433` 冻结根（`t.head_root` → `None`） | `ap_txn` **FAIL 9.12s**（"冻结读被并发 checkpoint 翻转"期望 12000 实得 12001）——冻结根半边受保护 |
| 探针 C | `ap_txn.rs` 临时补两条非 pk 断言（`WHERE v='upd'`→1、`WHERE v='v6'`→0）：修复在位 | **PASS 9.19s** |
| 探针 C′ | 同上断言 + 重放变异 1 | **FAIL 8.81s**（"txn UPDATE 经 AP 归并可读"期望 1 实得 0）——修复配方双向验证 |

**逐行读过的代码**（行号以 HEAD 为准）：`scan.rs` 398-408（路径优先级）、412-456（AP 门与冻结根）、497-549（三层归并/层③）、696-830（pk 下推 + 点查视图 R8-1 读己写）、859-920（table_scan 冻结根 + 层③）；`ap_txn.rs` 全文；`concurrent.rs` 全文；`ed1cf72` 全量 diff（含 sql_semantics.rs 删除段）。

**核对过的记账**：TASK.md Q-14 行、十三轮修复表五行、DoD 增补段；README:173；`docs/discussions/README.md` 队列；全仓 `allow(clippy` grep（31 处清单）；TASK.md Q-5（:124）/Q-16（:165）现状。

**读过的文档**：协议 README、第十三轮评审全文、第十三轮回应全文、第十二轮 §4.2/§4.3（G5 定义与上调口径）。
