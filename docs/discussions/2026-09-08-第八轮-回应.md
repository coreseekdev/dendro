# 回应：架构评审（第八轮）

- 回应人：dendro 开发者
- 日期：2026-09-08
- 对象：`2026-09-08-架构评审-第八轮.md`
- 门槛：`cargo test --workspace` 203 通过 / 0 失败；slt 9/9；clippy `-D warnings` 零输出

---

## 0. 总体态度

R8-1 是七个轮次以来对 SQL 正确性影响最大的发现之一——显式事务不读自己的
写，等于事务语义只实现了提交的一半。修复采纳评审建议的结构性方案：
table_scan 的键序双指针归并**收敛为三层 map 覆盖**（树 → checkpointed
overlay → 会话事务自身写），R7-1 那类键序错误在结构上无处可写；点查路径
同样覆盖自身写。

## 1. 修复

- **R8-1**：table_scan 单一归并抽象 + 点查自身写覆盖。
  回归 `r8_1_explicit_txn_reads_own_writes`：BEGIN;INSERT 可见；DELETE 后
  行消失且 UPDATE 空转不报错；UPDATE 读自己的写；COMMIT 后无幽灵行。
- **R8-5**：serve 入口初始化 tracing_subscriber（EnvFilter，RUST_LOG 可调，
  默认 info）——毒化/WAL 失败/checkpoint 失败等关键事件不再静默。
- **R8-6（部分）**：全部监听器 join，任一失败 exit(1)——bind 冲突不再
  "照常 ready 且永不退出"。优雅关闭（SIGTERM 语义）登记 Q-12。

## 2. 入册（本轮不实现，非遗忘）

- **Q-9（P1）** OCC 冲突检测窗口越过 checkpoint——评审探针的丢失更新
  场景成立。结构性修复（validate 回退树版本链 vs truncate 保留窗口）
  需要 memtx/truncate/树版本三方口径定案，列为下一批最高优先。
- **Q-10** 事务内 DDL 事务化（catalog 写集进 Txn）。
- **Q-11** 显式事务内 USE BRANCH 拒绝（25001，一行守卫，随下批小项）。
- **Q-12** 优雅关闭 + readiness/liveness 分离。

## 3. 基线（本地实跑）

203 通过 / 0 失败 / 1 ignored；slt 9/9；clippy `-D warnings` 零输出。
新增回归：r8_1（读自己的写全链）。
