# 架构评审（第十二轮·终评）：第十一轮收口验真（SHOW 放行 / use_branch 封堵 / Q-18 固化）+ 收敛终局判定 + 第二阶段最终排序 + 评审机制收尾建议

- 评审人：架构评审（外部）
- 日期：2026-09-08
- 对象：`2026-09-08-第十一轮-回应.md`、提交 `cc8cd0d`（第十一轮收口）、TASK.md R11 表
- 方法：逐行读收口 diff + 本地实跑 + **两探针 + 一 clippy 探针**（探针文件已删除，评审后 `git status` 干净，见文末方法说明）。委托点 a–c 逐项核查；按委托完成收尾总评：G1–G7 复评（§4.2）、收敛终局判定（§4.4）、第二阶段最终排序（§4.3）、评审机制收尾建议（§4.5）。
- 基线（评审人本机实跑，HEAD = `cc8cd0d`，工作树干净）：`cargo test --workspace` **212 通过 / 0 失败 / 1 ignored**；slt **9/9**；**`cargo clippy --workspace --all-targets -- -D warnings` ✅ 零输出**。**三个数字与回应声称逐位一致——十二轮以来首次。**

---

## 0. 总体判定（TL;DR）

**三个收口全部验真为真，且第一探针即符合声称**——R8-6（假修复）以来第一次"收口批次零折扣"：SHOW 放行豁免精确限定于 `BranchKind::Show`（探针证实 REOPEN 仍拒）、use_branch 守卫在任何状态变更前拒绝且成功路径语义完好（探针证实）、assembly 测试真实 spawn 二进制并断言四端口协议且随 CI 跑。第十一轮设定的收敛条件**全部满足**。

本轮新发现**全部为 P3 记账/注释级**（共 6 小项，无一项行为级）——连续第二轮零新增 P0/P1。最有意味的一处：记账勘误批自身漏了一处记账（TASK.md:172 仍引用不存在的测试名），说明"收尾记账粗糙"的长尾还在，但已不构成任何风险。

**终局判定**：主干正确性防线收敛——**确认**（边界条件见 §4.4）。G1 上调 ✅ 的条件（Q-18 入 CI）已满足，**G1 → ✅**。最终达成度：**G1 ✅、G2 ✅、G3/G4/G5/G6/G7 🟡**（🟡 的残余全部是第二阶段范围，不回滚主干收敛）。

---

## 1. 三个收口逐项验真（委托点 a）

### 1.1 收口①：SHOW BRANCHES 事务内放行 ✅

- **代码**：`sql/mod.rs:257-268`——守卫内 `if !matches!(kind, BranchKind::Show)` 豁免，其余五个 BranchKind（USE/CREATE/DROP/MERGE/REOPEN）维持 25001。豁免是白名单式而非一刀切移除守卫，形态正确。
- **回归**：`sql_semantics.rs:321` `r10_show_branches_allowed_and_snapshot_lifecycle`——事务内 SHOW 返回行 + **COMMIT 后 `active_snaps` 为空断言 + 同 watermark 双事务计槽 ≥2 / 先结束者不摘除他人保护 / 全部结束清空**。这就是 P11-3 要的 SQL 侧 R10-1/R10-3 常驻回归，"证据列引用 KV 测试充当 SQL 证据"的失实以"补上真证据"的方式关闭。
- **探针 P12-1**：事务内 `SHOW BRANCHES` 返回行、`REOPEN BRANCH b2` 仍 25001——**豁免未扩大化**（r9_2 现有用例清单不含 REOPEN，本探针建议转正补入，见 P12-6）。
- prepared 路径不受影响：分支语句 prepare 为文本透传（`sql/mod.rs:600-606`），执行时走同一 `exec_branch_statement`，豁免与拒绝在 wire 两侧口径一致。

### 1.2 收口②：Kv::use_branch 事务内拒绝 ✅

- **代码**：`kv.rs:66-71`——`self.txn.is_some()` 时在任何状态变更**之前**返回 25001（与 SQL 侧 Q-11 同口径）；旧代码的 `self.txn = None` 静默丢弃行已删除。P11-1 的"注册表泄漏 → 截断永久跳过"路径结构性封堵。
- **回归**：`kv_wire.rs:153` `kv_use_branch_inside_txn_rejected`——断言 state == "25001" 且随后 rollback 正常。
- **探针 P12-2**：非事务 use_branch 成功路径此前**零测试覆盖**（RESP `SELECT` 是唯一调用方，`kv_resp.rs:178`）——探针验证语义完好：main 写入 → 切 b2 不可见（隔离不串）→ 切回 main 原值仍在 → 注册表无残留。建议转正（P12-6）。
- wire 面自洽：RESP SELECT 走同一守卫，KV 客户端能看见 25001 而非静默丢事务。

### 1.3 收口③：Q-18 装配冒烟固化 ✅

- **代码**：`crates/dendro-server/tests/assembly.rs`——`CARGO_BIN_EXE_dendro`（:37）spawn 真实二进制，轮询 readyz 就绪后逐一断言：PG 端口 Startup→`'R'`（:100）、MySQL 端口连接即握手含 `dendro`（:104）、KV 端口 RESP PING→`+PONG`（:112）、metrics readyz→`HTTP/1.1 200`（:119）。**是进程级协议断言，不是单测模拟**——第十轮"FIFO 错位使 PG/MySQL 端口互换而全绿放行"的 P0 场景，此测试必然变红。
- **随 CI 跑**：`.github/workflows/ci.yml` test job = `cargo test --workspace`，集成测试含 assembly——声称结构成立。本机实跑该测试 ok。
- Q-18 划账属实（TASK.md），两次装配 P0 的结构性根因（装配层零测试）关闭。
- 三处 P3 小刺：blanket `#![allow(clippy::all)]`（P12-3）、readiness 注释失实（P12-4）、默认组合未自动化（P12-5）——见 §3，均不影响收口成立。

---

## 2. 基线与门槛（委托点 b）+ 流程整改核查（委托点 c）

**基线**：212/0/1（本机聚合计数，含 dendro-server 集成测试与 1 个 `#[ignore]` bench）——与回应逐位一致，十二轮首次（十一轮"208 实为 209"的口径失准就此终结）。209 + 3 新回归 = 212，账目自洽（回应"新增回归 4"中 kv_txn 为第八轮已计，实增 3，表述诚实）。slt 9/9 ✅。clippy `-D warnings` ✅。

**"replace 带 assert"整改**（委托点 c）：过程性声称，replace 脚本易逝、产物不可直接验。可验的代理指标有三，全部为正：
1. 失败史真实（第十轮 SHOW 修复被脚本化 replace 静默吞掉，第十一轮评审探针实锤——整改动机成立）；
2. 本轮 `cc8cd0d` 全部四项变更均带具名回归，commit 信息 / TASK R11 表 / 代码注释三处互相印证、可 grep 命中——"修复自带其触达层的测试"首次在本轮 diff 中全面可见；
3. 首探针即符合声称（三个收口无一需要第二轮验证）——R8-6 以来首次。

判定：**可信，但纪律尚不成文**。回应承诺"本轮起以脚本聚合计数为准"，仓库并无 scripts/ 载体（P12-1b）；建议把该纪律升格为成文规范（§4.5），否则"个人习惯"随时回退为"口头声称"。

---

## 3. 新增发现（本轮全部为 P3）

1. **P12-1（P3）记账勘误批自身漏了一处记账**：回应 §2 声称"第十轮证据列引用 KV 测试充当 SQL 证据的失实就此更正"——实际只通过新增 SQL 侧回归补齐了证据，**陈旧单元格未改**：TASK.md:172 R10-P0 证据列仍引用不存在的 `kv_txn_frozen_reads_and_decoded_values`（真实名为 `kv_wire.rs:90` 的 `kv_txn_reads_own_writes_and_decoded_values`，且该测试不断言注册表）。(b) 回应承诺"以脚本聚合计数为准"无仓库载体（无 scripts/）。"勘误批有漏项"是同一长尾模式的微缩重现，二场景修复均为分钟级。
2. **P12-2（P3）守卫注释测试名错误**：`sql/mod.rs:260` 引用 `sql_semantics::r10_show_branches_allowed_in_txn`，实际测试名为 `r10_show_branches_allowed_and_snapshot_lifecycle`（commit 信息与 TASK R11 表用的是正确名）。恰在"声称失实"主题的守卫注释里出现引用失实——建议证据引用一律以 grep 命中为准。
3. **P12-3（P3）assembly.rs:1 blanket `#![allow(clippy::all)]` 无必要**：clippy 探针（临时删除该行后 `cargo clippy -p dendro-server --all-targets -- -D warnings`）**零告警**——该豁免让整个文件游离于 clippy 门槛之外而毫无收益。建议删除，属防守性豁免反成门槛漏洞。
4. **P12-4（P3）assembly readiness 判定弱于注释**：`assembly.rs:64-71` 轮询把 readyz 响应读进临时 String 即丢弃，注释称"读一遍状态行判断 200"实际未判（就绪判定实际是"metrics 端口可连"——因 R9-3 前置 bind 而安全，但注释失实）；另 `pg_reply[0]`（:100）空响应时越界 panic，失败模式可读性差。最终断言处确有判 200，故仅为就绪判定与注释质量问题。
5. **P12-5（P3）装配自动化覆盖四端口显式组合，默认组合未覆盖**：README 快速开始路径（kv=0 的默认三端口）仍是十/十一轮人工实验的场景，未入 assembly 测试。建议补一个默认组合用例（spawn 不带 --kv-port，断言 pg/mysql/metrics 三端口）。
6. **P12-6（P3）两条探针建议转正**：REOPEN-in-txn 25001 补进 r9_2 用例清单；use_branch 成功路径隔离语义入 kv_wire（各 10 分钟级）。

---

## 4. 收尾总评（十二轮终评）

### 4.1 十二轮轨迹一段话

R1–R4 建立协议与地基，R5–R10 每轮必出新缺陷（keepalive 回归、假修复、注销缺失、双装配/生命周期 P0），R11 首次零新增 P0，R12 连续第二次且新发现全部降至 P3。"修复引入新缺陷"的模式连续两轮未再现；行为级声称连续两轮 100% 可信；基线数字首次逐位精确。收敛不是宣布出来的，是这条轨迹走出来的。

### 4.2 G1–G7 最终达成度（终评）

| 目标 | 十一轮 | **十二轮终评** | 依据 |
|------|:----:|:----:|------|
| G1 双协议分离 | ✅（附条件） | **✅（条件已满足，终局）** | 条件"Q-18 自动化入 CI"已交付并验真（§1.3）；防线从人工转为 CI 资产。三处 P3（P12-3/4/5）不构成回弹风险等级 |
| G2 只写+版本化+O(1) 分支 | ✅ | **✅（维持）** | 写路径十二轮零新缺陷；use_branch 封堵后生命周期无已知洞 |
| G3 云原生 | 🟡（收窄） | 🟡（不变） | 残余：Q-12 优雅关闭（k8s 硬伤）、P1-8 真崩溃、弹性调度基准零——全部第二阶段 |
| G4 列存为 GPU 保留 | 🟡 | 🟡（不变） | M-4 连续十二轮零工作 |
| G5 SQL 一等公民 | 🟡（接近收敛） | 🟡（边界收窄至最后一项） | SHOW 例外清账后，距 ✅ 仅差 Q-14（AP 路径事务语义）与 Q-16/Q-5 边角 |
| G6 压缩+量化衰退 | 🟡 | 🟡（不变） | M-3 连续十二轮零工作 |
| G7 性能绝对优先 | 🟡 | 🟡（不变） | clippy/内存上界改善延续；欠 P2-3 方法学、BEGIN×commit_mu 尾延迟观测 |

### 4.3 第二阶段路线最终建议排序

| 序 | 项 | 排序理由 |
|:--:|------|------|
| 1 | **Q-14** AP 路径读己写 + 冻结根 | 显式事务矩阵最后一个正确性洞；G5→✅ 的唯一实质门槛。复用 table_scan 三层归并抽象，勿写第二套 |
| 2 | **P1-7** 真并发 OCC | **收敛判定的边界条件**（§4.4）——并发执行模型尚未被检验，若此处有洞则终局判定需修订；宜早不宜迟。现成四用例，顺带观测 BEGIN×commit_mu 尾延迟（G7 在册欠账） |
| 3 | **Q-15** 文档总清账 | 连续四轮产出评审发现的最大持续来源；本轮再实证两处（P12-1/2）。含 SPEC 04 §3 重写（commit_tx 管线 + Q-9 双层口径）、新增 SPEC 11（KV）、TASK 证据列勘误（P12-1）、README 状态段。1–2 天还清 |
| 4 | **Q-12** 优雅关闭 + readyz 分离 | 小时级工作量、k8s 终止语义硬伤——性价比最高的插队项，可随任一批顺带 |
| 5 | **P1-8** 真崩溃（SIGKILL） | 云原生底座证据；"drop(db) 模拟"从未被真 kill 检验 |
| 6 | **Q-1 → Q-13 ↔ Q-9** | 内存上界叙事闭环三件套：读内存上界 → 保留精细化 → 树版本链，联动定案防返工 |
| 7 | **Q-10** 事务化 DDL | 根治 25001 保守守卫——r9_2/r11 豁免清单整体退役为正常事务语义，是 SHOW 例外这类"保守口径利息"的终结者 |
| 8 | **M-3 / M-4** | G6/G4 唯二无法上调的直接原因（连续十二轮零动）；至少先各做一条最小测量曲线 |
| 9 | **P2'-2** trait 化（随 P3-1 决策）/ **P2-1** slt 语料 | 结构性投资与基线广度，不抢正确性批次 |

**前三**：Q-14、P1-7、Q-15。逻辑：先补最后一个正确性洞（Q-14），随即检验收敛判定的边界（P1-7），再用一次清账把评审发现的最大来源归零（Q-15）——三批做完，"产品级"叙事里正确性、并发、文档三个支柱全部立住。

### 4.4 收敛终局判定（本轮核心问题的最终回答）

**写路径 / 读路径（行式 + KV 点查/扫描）/ 装配与生命周期——在单会话编排执行模型下的正确性防线：收敛，终局确认。**

依据：① 第十一轮设定的三个收口全部验真落地（§1），且各自带常驻回归入 CI——"收敛从评审判断变成受测试保护的常态"这一条件 literal 满足；② 连续两轮零新增 P0/P1，"修复引入新缺陷"模式连续两轮中断；③ 机制形态（引用计数注册表 + 全路径注销 + commit_mu 互斥注册 + 按名装配）经两轮独立探针检验无新洞，十轮迭代抵达的终点形态稳定；④ 首探针即符合声称，声称可信度已达"无需折扣"。

**边界（必须随判定一并声明）**：
- 真并发（P1-7）与真崩溃（P1-8）执行模型未检验——收敛声明的适用范围以已检验的执行模型为界；P1-7 排序第 2 正是为尽早检验此边界；
- AP 列存路径（Q-14）在事务语义之外，属"已知且已登记的洞"，不属"未知风险"；
- 收敛 ≠ 无 bug，等于：已知的每一条路都有机制、有测试、有验证记录，且防线随 CI 常驻。

### 4.5 评审机制收尾建议（委托的明确回答）

十二轮 Review+Fix/Impl 循环已完成其历史使命——它把一个"装配层零测试、生命周期有结构性洞、声称与事实脱钩"的引擎带到了"防线常驻 CI、声称零折扣"的状态。**建议停止固定节拍的全文评审，转为双触发制：**

1. **里程碑触发（主）**：每完成 §4.3 的一个批次，做一次**半天级聚焦复评**——只审该批 diff + 新声称 + 新回归的"人为回退变红"抽查（本轮 §1 的方法即可，不再逐行全文重审）。首个预约：**第 13 轮 = Q-14 + P1-7 完成后**（核对事务矩阵 SPEC 04 成表 + 并发用例 + AP 冻结读，预计轻量）。
2. **例外触发（辅）**：出现 P0、行为级假声称、或 CI 防线回弹且修复不自带测试时，随时插入专项评审，不受里程碑约束。
3. **日常守护移交 CI**：test / slt / clippy / assembly 四件套已在 CI——这是十二轮最重要的机制遗产。评审退居"抽查 + 里程碑终评"，日常回归由 CI 说话。
4. **三条成文规范**（把本轮的可信度从个人习惯变成制度）：
   - DoD 增补第 4 条：**修复必须自带其触达层的测试**（十轮提出、十二轮才在 diff 中全面兑现的纪律，落成文字）；
   - TASK.md 证据列只允许引用 **grep 可命中的测试全名**（P12-1/P12-2 的共同根因）；
   - 基线三数脚本化落仓（`scripts/baseline.sh`：聚合 test 计数 + slt + clippy 一次输出）——兑现"以脚本聚合计数为准"的承诺载体。

---

## 附：评审方法说明

**本机实跑**（HEAD = `cc8cd0d`，评审前后工作树均干净，探针文件与全部临时产物已删除）：
- `cargo test --workspace`：**212 passed / 0 failed / 1 ignored**（1 ignored = `bench_cbf.rs:67` 在册 bench）
- `cargo run -q -p slt -- run tests/slt/dendro`：**9/9**
- `cargo clippy --workspace --all-targets -- -D warnings`：**零输出（绿）**
- `cargo test -p dendro-server --test assembly`：单独复跑 ok（spawn 真进程，四协议断言全过）

**探针实验**（临时测试 `crates/dendro-core/tests/probe_r12.rs`，两用例，运行记录后删除）：
- P12-1（✅收口①边界）：事务内 `SHOW BRANCHES` 返回行（豁免生效）、`REOPEN BRANCH b2` 仍 25001（豁免未扩大化）。
- P12-2（✅收口②边界）：非事务 `use_branch` 成功路径语义完好——main↔b2 键空间隔离不串、往返后原值仍在、注册表无残留条目。

**clippy 探针**（临时删除 `assembly.rs:1` 的 `#![allow(clippy::all)]` 后运行 `cargo clippy -p dendro-server --all-targets -- -D warnings`，随后 `git checkout` 还原）：**零告警**——证明该豁免当前无必要（P12-3）。

**逐行读过的代码**（行号以 `cc8cd0d` 为准）：`sql/mod.rs`（守卫与豁免 253-268、USE 守卫 272-275、REOPEN 分派 372-381、prepared 分支语句透传 600-606）；`kv.rs`（use_branch 守卫 66-71、旧 `self.txn = None` 已删——diff 确认）；`tests/assembly.rs` 全文；`sql_semantics.rs`（q11/r9_1/r9_2/r10_* 四组回归全文）；`kv_wire.rs`（kv_use_branch_inside_txn_rejected、kv_txn_reads_own_writes_and_decoded_values）；`.github/workflows/ci.yml` 全文；`dendro-server/Cargo.toml`（bin 名 dendro，CARGO_BIN_EXE 有效性）；`kv_resp.rs:178`（RESP SELECT 走同一守卫）。

**核对过的记账**：TASK.md（R11 表四行、Q-18/Q-20/M-1/M-2 划账、R10 表证据列——发现 P12-1 残留）、README M7 行 9/9、kv.rs:1 SPEC 缺号标注、`grep` 全仓确认 `kv_txn_frozen_reads_and_decoded_values` 不存在、scripts/ 目录不存在（P12-1b）。

**读过的文档**：协议 README、第十一轮评审与回应全文、TASK.md 相关段落。
