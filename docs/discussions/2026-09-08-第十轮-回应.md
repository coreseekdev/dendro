# 回应：架构评审（第十轮）

- 回应人：dendro 开发者
- 日期：2026-09-08
- 对象：`2026-09-08-架构评审-第十轮.md`
- 门槛：`cargo test --workspace` 208 通过 / 0 失败；slt 9/9；clippy `-D warnings` 零输出

---

## 0. 总体态度

两个 P0 全部成立，且都在我第九轮的修复里——"修复引入新 P0"第三次发生，
模式警报接受。共性根因正如评审指出：**装配层与生命周期层零测试覆盖**，
交付验证止于单测绿。本轮修复 + 新增针对性回归（含一个此前从未验证的
成功路径断言：端口↔协议对应）。

## 1. P0 → ✅

- **R10-1 快照注销缺失**：COMMIT、ROLLBACK、failed-COMMIT 三条路径全部
  注销（此前只有 Session::drop 且仅未决事务）。
- **R10-2 监听器装错端口**：listeners 改 **HashMap 按名存取**（remove("pg")
  等）——FIFO 错位从结构上不可能。进程级验证：PG 端口回 AuthenticationOk
  ('R'+ParameterStatus），MySQL 端口回 8.0.36 握手（成功路径首次被断言）。

## 2. P1/P2 → ✅

- **R10-3**：`active_snaps` 改 `BTreeMap<u64, usize>` 引用计数——同
  watermark 并发事务各持一槽，先结束者不再连带摘除他人保护（评审探针
  P10-2 的击穿场景关闭）。
- **R10-4**：KV 显式事务接入两套机制——begin 注册活跃快照 + 冻结根
  （kv_entry 以冻结 catalog 根解析）；commit/rollback/Drop 注销。
  回归 `kv_txn_frozen_reads_and_registry_lifecycle`（冻结读 + COMMIT 后
  注册表空断言）。
- **P10-5**：covered_min **只在真截断时推进**——保留窗口内的提交不再被
  误拒 40001（冲突检测有完整 memtx 历史，跨 checkpoint 事务可正常提交，
  语义比此前更准）。r7_3/q9 回归仍绿。
- clippy 两个 unused 已修；R9-2 守卫放行只读 SHOW BRANCHES。

## 3. 登记与制度

- **Q-18（P1）** 装配层测试为零的结构性整改入册：serve 装配函数化 +
  端口↔协议对应的进程级冒烟测试入 CI；
- **Q-19** "线程不持强 Arc 睡觉/自环"红线入 AGENTS.md（Q-19 登记）；
- R9 轮未登记的 R9-6/R9-9/R9-11 补录（Q-20）；
- 第五轮起的模式判断（装配/生命周期层是回归高发区）接受；G1/G7 评分
  待本轮修复后由评审复评。

## 4. 基线（本地实跑）

208 通过 / 0 失败 / 1 ignored；slt 9/9；clippy `-D warnings` 零输出。
新增回归：kv_txn_frozen_reads_and_registry_lifecycle（冻结读 + 注册表
生命周期）；端口↔协议对应为进程级手动验证（自动化入 CI 列 Q-18）。
