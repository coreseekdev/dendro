# 回应：架构评审（第十四轮轻量复评）

- 回应人：dendro 开发者
- 日期：2026-09-08
- 对象：`2026-09-08-架构评审-第十四轮-轻量复评.md`
- 门槛（脚本计数）：`scripts/count-tests.sh` → workspace **218 通过 / 0 失败** / 1 ignored；slt 9/9；clippy `-D warnings` 零输出

---

## 0. 总体态度

R14-1 判定接受：pk 等值断言全部被 pk 下推截走、走行路径的 R8-1 读己写，
AP 归并层③禁用后测试仍全绿——"事务矩阵全链"的声称强于证据。评审配方
（两条非 pk 断言）已按方落地，并自跑变异自检（禁用层③ → 红在目标断言；
还原 → 绿）。

## 1. 修复

- **R14-1**：`ap_txn.rs` 补 `WHERE v='upd'`→1 与 `WHERE v='v6'`→0 两条
  非 pk 断言（变异自检：禁用 AP 层③时前者得 0、后者得 1，测试红在目标
  断言）。
- **R14-2**：一次性清扫**全仓 13 个测试文件**的 blanket
  `#![allow(clippy::all)]`（含 ap_txn.rs 新建时复制模板引入的那处——
  "复制模板"归因第三次应验）+ clippy 修正（useless format、strip_prefix
  惯用法、let-and-return 等）。测试代码自此纳入 clippy 门槛。

## 2. G5

两条非 pk 断言 + 变异自检通过后，Q-14 的证据门槛（人为回退变红）达成——
G5 上调 ✅ 的全部条件已具备，请终审确认。

## 3. 基线（脚本计数）

`scripts/count-tests.sh`：**218 通过 / 0 失败 / 1 ignored**；slt 9/9；
clippy `-D warnings` 零输出（测试代码首次纳入门槛）。
