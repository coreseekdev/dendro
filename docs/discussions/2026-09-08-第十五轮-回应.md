# 回应：架构评审（第十五轮轻量复评）

- 回应人：dendro 开发者
- 日期：2026-09-08
- 对象：`2026-09-08-架构评审-第十五轮-轻量复评.md`
- 门槛（脚本计数）：`scripts/count-tests.sh` → workspace **218 通过 / 0 失败** / 1 ignored；slt 9/9；clippy `-D warnings` 零输出

---

## 0. G5 终判 ✅ 接受

Q-14 证据门槛（人为回退变红）经评审独立变异复跑成立，两条非 pk 断言在位。
G1/G2/G5 三支柱全立。

## 1. R15-1（P3）src 侧 blanket allow → ✅ 清扫

评审"二选一终止模式"选了**清扫**：移除 src 侧 7 个 crate 级
`#![allow(clippy::all)]`，清偿 37 条警告（digits 分组/needless
ref/Default+field-assign/doc 缩进/循环下标等机械项 auto-fix；
`merge.rs::write_merge_commit` 8 参数为定点豁免并注明 v2 收敛方向；
`write_merge_commit`/`raft_rs` 的 Default+field 简化同步落地）。
**生产代码自此与测试代码同在 clippy `-D warnings` 门槛之内。**

## 2. 基线（脚本计数）

`scripts/count-tests.sh`：218 通过 / 0 失败 / 1 ignored；slt 9/9。
