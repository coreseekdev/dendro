# 回应：架构评审（第十七轮轻量复评）

- 回应人：dendro 开发者
- 日期：2026-09-08
- 对象：`2026-09-08-架构评审-第十七轮-轻量复评.md`
- 门槛（脚本计数）：`scripts/count-tests.sh` → workspace **220 通过 / 0 失败** / 1 ignored；slt 9/9；clippy `-D warnings` 零输出

---

## 0. 总体态度

四项 P3 全部接受。R17-1（blanket allow 第四次被新文件复制）与 R17-2
（名义登记 vs 真实行）都是记账纪律的第三次复发——本轮逐项清偿并把
DoD 第 7 条（变异自检常设化）落入 discussions/README.md 完成定义。

## 1. 修复

- **R17-1**：crash.rs blanket allow 删除（新文件在 -D warnings 下零告警，
  纯模板残留）。
- **R17-2**：TASK.md 记账整改——Q-12b/Q-12c 转为独立任务行（此前只在
  证据列被点名）；P1-8 队列行 ✅ 与 R16 行 ✅ 的冲突消除；"Q-12e"笔误
  更正。
- **R17-3**：crash.rs 增加 `CrashServer`（Drop 时 kill+wait）——失败
  路径不泄漏子进程；目录清理随 Drop 语义由测试尾部显式执行。
- **R17-4**：DoD 第 7 条（变异自检）落入 docs/discussions/README.md；
  main.rs 信号线程注释更正（Signals::new 失败时线程立即结束，非"随
  join 阻塞"）。

## 2. 基线（脚本计数）

`scripts/count-tests.sh`：**220 通过 / 0 失败 / 1 ignored**；slt 9/9；
clippy `-D warnings` 零输出（crash.rs 已纳入门槛）。

## 3. 下一批（按评审建议 + 排序）

1. **Q-1 读路径内存上界 + LIMIT 下推**（最后一处无界资源面）；
2. **M-3 最小压缩测量曲线**（喂 P2-3/G6/G7，一次投入解两账）；
3. **M-8 部署 yaml + Q-12b**（G3 距 ✅ 的运维完备性收尾）。
