# 架构评审（第十五轮·轻量复评）：R14-1/R14-2 双双验真——G5 上调 ✅

- 评审人：架构评审（外部）
- 日期：2026-09-08
- 对象：`git log ed1cf72..HEAD` 单笔提交 `43a4ac0`（R14-1 非 pk 断言 + 变异自检；R14-2 blanket allow 清扫）
- 方法：按十二轮 §4.5 双触发制执行里程碑聚焦复评。**评审人独立复跑变异自检两枚 + 还原**，另实跑脚本计数与 clippy 门槛。终态 `git status` 干净（`git diff` 空，HEAD = `43a4ac0`），无变异/探针残留，见文末。
- 基线（评审人本机实跑）：`./scripts/count-tests.sh` → **workspace 218 / 0 / 1 ignored；slt 9/9**；`cargo clippy --workspace --all-targets -- -D warnings` **零输出**。与 commit message 声称逐位一致——DoD 第 5 条连续第三轮兑现。

---

## 0. 总体判定（TL;DR）

**R14-1 ✅（验真）**：两条非 pk 断言在位（`ap_txn.rs:60-64` / `:65-69`），且带机理注释（`:56-59`）。评审人独立复跑委托验收判据：禁用 `scan.rs:528` 层③ → **红在目标断言 `ap_txn.rs:64`**（"事务内 UPDATE 的新值必须经 AP 归并可见" 期望 1 实得 0）；还原 → 绿。第十三轮即已定位、第十四轮已双向验证的病灶，至此**修复、断言、灵敏度三要素齐备**。

**R14-2 ✅（验真）**：全仓 grep 显示 tests/ 目录 blanket `#![allow(clippy::all)]` **零残留**（提交精确清扫 13 个测试文件：core 6 + server 7）；clippy `-D warnings` 实跑零输出。测试代码自此真实处于 clippy 门槛之内。

**G5 终审：上调 ✅**。按第十四轮定义（"距 ✅ 仅差 Q-14 证据门槛；Q-16/Q-5 不阻门"），证据门槛本轮由评审人独立复核成立。七项目标达成度：**G1 ✅、G2 ✅、G5 ✅、G3/G4/G6/G7 🟡**（🟡 余项均为第二阶段范围）。

新发现：**1×P3 观察项（R15-1，非缺陷）**，无行为级缺陷。

---

## 1. R14-1 验证——**✅**

- **断言在位**：`ap_txn.rs:60-64`（`SELECT count(*) FROM big WHERE v = 'upd'` → 1）与 `:65-69`（`WHERE v = 'v6'` → 0），非 pk 谓词、强制走 CBF 归并路径，正是第十四轮探针 C 配方原样；`:56-59` 注释把"pk 下推截走 / 缺失层③分别退化为 0 行 / 1 行"的机理写进了测试文件本身——第十四轮建议的"固化"以注释形式落地。
- **变异自检（评审人独立复跑，非转述研发声称）**：

| 编号 | 变异 | 结果 |
|---|---|---|
| M1 | `scan.rs:528` 层③整体禁用（`if t.explicit` → `if false && t.explicit`） | **FAIL 10.05s，红在目标断言 `ap_txn.rs:64`**（期望 1 实得 0）——委托验收判据成立 |
| M2 | 仅禁用层③ Delete 臂（`scan.rs:539-541` `keyed.remove(k)` → `let _ = k;`） | FAIL 8.72s，红在 `ap_txn.rs:48`（AP 路由 count，12001≠12000）——删除半边的灵敏度由 AP count 先行承担，`v='v6'` 为纵深防线 |
| 还原 | 两次逐一还原 | `ap_txn` PASS 8.95s；`git diff` 空 |

- 判据链条闭合：M1 证明"层③回退必红"（第十四轮缺失的那半边），M2 证明 UPDATE/DELETE 两半各自有独立的回退灵敏度，还原绿证明断言测的是实现而非环境。

## 2. R14-2 验证——**✅**

- **全仓 grep**：`#![allow(clippy::all)]` 在 `crates/*/tests/` **零命中**；提交 diff 精确移除 13 个测试文件各 1 行（与"13 个测试文件"声称一致）。剩余三类豁免全部清点：
  - src 侧 **7 个 crate 级 blanket allow**（dendro-core/src/lib.rs:7、dendro-columnar/src/lib.rs:48、dendro-pgwire/src/lib.rs:19、dendro-mywire/src/lib.rs:14、dendro-server/src/lib.rs:2、dendro-server/src/main.rs:1、slt/src/main.rs:1）→ 见 R15-1；
  - 10 处 crate/item 级 `allow(clippy::type_complexity)`——定向豁免，存储引擎复杂类型属合理使用；
  - 3 处 tests/common/mod.rs `allow(dead_code)`——共享测试脚手架，定向合理。
- **clippy 实跑**：`cargo clippy --workspace --all-targets -- -D warnings` 零输出。
- **修正抽查**：`wal_corruption.rs`（doc continuation）、`wire_clients.rs`（let-and-return、`tokio::spawn(conn)` 惯用法）逐处核对正确。

**R15-1（P3，观察项，非缺陷）：clippy 门槛目前只覆盖测试代码，生产代码整体豁免。** 上述 7 个 src 侧 crate 级 blanket allow 使全部生产 lib/bin 对 clippy 不可见——"测试代码首次纳入 clippy 门槛"的声称准确，但其补集恰是：门檻对 src 零覆盖。测试侧的三轮"打地鼠循环"（P12-3 → P13-3 → R14-2）在本笔终止，同一模式在 src 侧仍是现状。不阻任何事项，建议下一步**要么同法清扫 src 侧并以 clippy 探针定案（预期工作量小时级，type_complexity 可保留为定向豁免），要么在 AGENTS.md 成文"src 侧 blanket allow 属已知接受"**——与第十四轮同款二选一，勿让循环在 src 侧重演。

## 3. G5 上调终审——**✅（研发声称成立）**

| 门槛要件 | 判定 | 依据 |
|---|---|---|
| Q-14 实现（AP 读己写 + 冻结根） | ✅ | 十三轮探针 B、十四轮探针 C、本轮 M1/M2 变异红——四轮四度侧证 |
| Q-14 回归（CI 防线随常驻） | ✅ | 本轮 M1：层③禁用 → 红在目标断言；M2：Delete 臂禁用 → 红在 AP count——两条灵敏度均由评审人独立复跑确认 |
| Q-16/Q-5 阻门？ | 不阻（维持十四轮口径） | Q-16 为引擎级唯一性检查缺失（行/AP 共享写集，非 AP 分叉）、Q-5 为协议工效项，均属边角，继续在册清偿 |

**G5 → ✅。边界声明**：SQL 一等公民的语法/语义基线（slt 9 文件）+ AP 事务一致性（q14 双向灵敏度）均有 CI 防线；Q-16/Q-5 为非阻门边角、在册清偿。

记账建议（随回应处理，分钟级）：G5 状态目前只存在于十二轮评审 §"最终达成度"与历轮复核文中，TASK.md / SPEC 00 无落点——建议研发在本轮回应中把 G5 ✅ 及上述边界声明落一行进 TASK.md（或 SPEC 00 状态注记），使目标达成度有权威台账。

## 4. 其余记账与遗留小刺——**✅（两处脚注，不立项）**

- 基线数字逐位一致（218/0/1、slt 9/9、clippy 零输出）；TASK.md 新增"十四轮修复"表与实现一致；README "SQL 基线（9 文件）"一致。
- 全仓无 TODO/FIXME/unimplemented 残留；`wal_corruption.rs` 头部出现连续双空行（纯外观，无门槛约束，不值一项）；AGENTS.md:4 "无 clippy/rustfmt 配置"描述与现状仍一致（门槛靠命令行 `-D warnings`，非配置文件）。

## 5. 下一步建议（按序）

1. **G5 落账**（分钟级）：TASK.md 记 G5 ✅ + 边界声明，回应本报告。
2. **R15-1 收口**（小时级）：src 侧 7 处 blanket allow 清扫或成文接受（二选一，终止模式）。
3. 之后按十二轮 §4.3 继续：**Q-12 优雅关闭 → P1-8 真崩溃**。G1/G2/G5 三支柱全立后，主干侧再无 🟡 正确性项；剩余 G3/G4/G6/G7 属第二阶段性能/云原生量化范畴。

---

## 附：评审方法说明

**本机实跑**（HEAD = `43a4ac0`，评审终态 `git status` 干净、`git diff` 空、无 MUTATION/PROBE 残留）：

- `./scripts/count-tests.sh`：**workspace 218 / 0 / 1 ignored；slt 9 文件全绿**
- `cargo clippy --workspace --all-targets -- -D warnings`：**零输出**
- `cargo test -p dendro-server --test ap_txn`：还原后 PASS（8.95s）；两枚变异各自复现红（10.05s / 8.72s），均已逐字节还原

**核对过的材料**：`43a4ac0` 全量 diff（17 文件）；`ap_txn.rs` 全文；`scan.rs:497-549`（层③）；TASK.md 十四轮修复表；README:168-179；AGENTS.md；全仓 `allow(clippy` / `#![allow` / TODO 三类 grep；十二轮 G5 定义与十四轮上调路径原文；docs/discussions/README.md 队列。
