# 08 里程碑、风险与待决问题

## 0.4 v2c-1a/b 落地记录（2026-09-15）

- **1a（C1/C4）**：Chunk 类型（len=逻辑行数/零行不变量/边界转换器）；
  dispatch_scan 纯函数 + ap_resolve 单点资格 + force_source（调试构建）；
  dispatch_differential 5 测试。**首跑抓两潜伏 bug**：账本 #24（AP 单边
  pk 范围 0 行——Option 序陷阱三处）、#25（I-H1 差分自成立以来空转）。
- **1b（C3/C5）**：push 管线协议（FlowControl 三态二分/finish-EOS/停止
  令牌随 Ctx/零批跳过/metrics 位）+ FilterOp（eval_chunk v1=行循环）+
  CollectSink（LIMIT 截断-Stop）+ drive；WHERE 阶段接线（>64 行走管线，
  ≤64 走紧循环——bench 实证小结果集上管线包装常数不可接受）。
  **载荷泛型**（行批/Chunk 共协议）——与 04 "Chunk 唯一货币"的偏差在此
  记录：TP 行路径免 arrow 物化（85% 线纪律优先），AP 侧维持 Chunk。
- bench 注记：机器负载漂移（miss 臂 115k→90k 滑落），验收线评估以
  hit/miss 比（≈2.5，历史稳定）为准；A/B 实证管线路径无回归（250k vs
  全关 219k 同机同刻）。

## 0.2 评审记录（v1.2，2026-09-15 双 agent 对照审视）

**DuckDB/Polars 本地源码对照（D 报告）**——7 必须吸收已回写：
D1 PipeOp 补 finish/EOS（Aggregate/Sort/HashJoin 收尾的接口前提，04 §2）
+ HashJoin 拆 build/probe 两段；D2 停止令牌随 Ctx（跨线程后返回值传播
失效的预留）；D3 零行 Chunk 不变量；D4 len=逻辑行数定死；D5 **v2 列式
寄存器 = 类型化值缓冲 + null bitmap 现在写死**（SqlValue 内嵌 NULL 是
向量化死路）；D6 常量提升出循环（无需常量向量编码吃到主收益）；D7
驱动器 metrics 挂点。可选吸收：O1 AND 链 sel 压缩/谓词重排**绑定进
Kleene 化 PR**（与 eager 红线冲突，不独立排期）；O7 debug Verify（07 §4.5）。
**确认一致 10 项**：push+FlowControl 单线程形状、chunk 级 sel 粒度、
4096、不做多编码、eager 红线、步列表兼容两实践、文本 IR 三合同超
DuckDB/Polars。

**PG/SQLite/MySQL 机制对照（P 报告）**——7 必须吸收已回写：
M1 **prepared 执行期重校验**（正确性：模板化首次引入失效义务，06 §1.1；
SQLite prepare_v2/PG RevalidateCachedQuery/MySQL 自动重准备三重同构）；
M2 **CatalogCache 随 manifest version 换新**（正确性：col_segments 非
schema，挂错键=漏读/扫退休段，05 §5.5+06 §3.5）；M3 步的 NULL 边界
语义表 + Qual 终结步 + JumpIfFalse 更名 JumpIfNotTrue（03 §5.5）；
M4 builtin strict 元数据（03 §1 + 04 聚合合同）；M5 required_cols 位图
+ 缺列补 Null 进 Source 合同（02 §3.6/04 §4）；M6 寄存器约定 +
分配诚实声明 + 步尺寸纪律（03 §3）；M7 文档纠错（schema cookie 归属
SQLite；LogicAnd 注释矛盾）。可选：O1 digest 规整化（Q14）；O2 派发
不缓存的免疫显式化（已写 05）；O4 视图 provenance（已写 02）；O5 L1
分桶（Q15）；O6 每会话 prepared 上限（已写 06 §4）；O7 CTE/Subplan
预留位（03 §5.6/02）；O8 派发计数器（已写 05 §5）。
**确认一致 10 项**：enum+match 单循环（PG10 抛弃函数指针的同方向）、
每表达式一程序粒度、branch 入键=search_path 快照同构、Param 步同构、
聚合出算子层、拒 JIT 论据、文本 IR 克制度=SQLite 原生 EXPLAIN 同款。

## 0.1 评审记录（v1.1，2026-09-15 GLM-5.3 评审 agent）

3 P0 + 8 P1 全部采纳并已回写各文件：P0-1 三值逻辑改现状复刻（03 §4
重写，Kleene 化列为显式未来变更）；P0-2 Delta 层构成纠正（02 §3
CurrentPoint/CurrentRange 四段合成 + 04 §4 重写）；P0-3 Param 步 +
prepared 模板缓存（03 §1 / 06 §1.1）。P1：06 失效矩阵闭合 + version
滞后前置修复；02 §3.5 绑定前置特判 + 10 号 spec；02 §1 表达力补齐
（OFFSET/HAVING/AggCall/SortKey/Subquery）；11 号 spec DML 边界；07
§1.1 结果等价定义前置；05 §5.5 catalog 快照；04 §2 错误通道；03
§7.5 附录 A 前置 + LIKE 重分类为新功能。新增 Q9-Q12。

**前置小 PR（B1 之前合入，五个）**：
1. update_manifest 快照 version 滞后修复（P1-1）；
2. 结果等价正式定义 helper（07 §1.1）；
3. Expr→现状语义清单（03 附录 A）——对拍基准；
4. **B0-a** vendor sqlparser-rs PG/MySQL 方言语料子集（P 层回归门，
   parser 升级验收）；
5. **B0-b/c** PG 回归语料 parse-only 冒烟 + cargo-fuzz parse_batch
   （分层测试集详见 docs/research/SQL解析测试集调研.md）。

## 1. 里程碑

### v2b —— 标量层（独立可交付，先行）

| 项 | 内容 | 验收 |
|----|------|------|
| B1 | `ScalarStep` 编译器（绑定+折叠+lowering，03 §2）| 编译器单测 R1-R3 |
| B2 | `eval_row` 后端接入 exec（替换 Filter 谓词的 AST 求值路径）| slt 全绿 + **编译产物 vs 现 AST 求值逐表达式对拍**（评审修正：force_source 是 C4/v2c-1 交付物，B2 时序上不可用）|
| B3 | ~~两级计划缓存~~ → schema_version 白名单 + prepared 重校验 + L1 分桶（06 §7：L2 推迟至 v2c-1，WHERE 编译开销实测微小）| schema_version 4 测试（DDL bump 矩阵/数据不 bump/checkpoint 不 bump/prepared ALTER 后重编译）|
| B4 | EXPLAIN 步列表段 | R4 round-trip |
| B5 | 步列表缓存（1024 上限 + 步数硬断）| S-3 有界测试 |

**不包含**：eval_chunk、Chunk、Source 改造（那是 v2c-1）。
**工作量估计（评审后 2.5×修正）**：编译器+双后端+缓存+文本格式+
EXPLAIN ≈ 1500 行核心 + 附录 A 映射表 + 测试 ~2000 行；
schema_version 机制 ~150 行 + 回归。

### v2c-1 —— ChunkPlane 骨架

| 项 | 内容 | 验收 |
|----|------|------|
| C1 ✅ | Chunk 类型 + 边界转换器 | round-trip 单测 ✓ |
| C2 →v2c-2 | 四类 Source 接 Chunk **顺延**：try_ap_scan 的段循环/去重/overlay 归并在返回前完成——Source 化即归并源重构本体，拆开做=同一逻辑写两遍（违背"搬运非新逻辑"）。随 v2c-2 MainPlusDelta 归并源整体交付 | — |
| C3 ✅ | 管线协议（FlowControl 三态/finish-EOS/停止令牌/零批/检查点）| 5 单测 ✓（LIMIT 截断短路/错误上抛/取消/空批）|
| C4 ✅ | 派发器 + force_source + EXPLAIN 派发理由行（含 forced 标注）| 差分 6 测试 ✓（点/范围/全表/AS OF 臂/结构不可行错误面/EXPLAIN）；micro-bench ≤200ns 顺延至 CatalogCache（05 §5.5）落地时一并（当前 resolve 未收敛）|
| C5 ✅ | eval_chunk v1（行循环 = FilterOp）| 与 eval_row 同源（B1 差分即其合同）+ 端到端差分 |

**工作量估计（评审后 2.5×修正）**：~3500 行核心（scan.rs 2608 行的
重构面）+ 差分基建 ~1500 行。
**风险最高项**：C2/C4——行为等价的主战场。

### v2c-2 / v2c-3 —— 优化（见父文档 §4）

**v2c-2 进展**：MainPlusDelta 归并源 ✅（段旧→新 BTreeMap 覆盖 + overlay
+ txn 三路归并；输出恒 pk 有序——路径相关序来源之一收敛；pushdown_limit
两分支同口径；dispatch_differential 7 测试 + ap_tp 3 + slt 26 护航）。
语义债前置清偿 ✅（#21/#22/#23 join/聚合 + #26 字面量三处数值化源头）。
剩余：算子群（Project/Agg/Sort + OpAsSink 链）+ Source 层 Chunk 流式接入。

v2c-3：段增量物化 + 存储字节比 bench。

## 2. 风险表

| # | 风险 | 概率 | 缓解 | 触发信号 |
|---|------|------|------|---------|
| R-a | TP 点查不达 85% 线 | 中 | 退化管线（无中间算子）+ 线程本地复用 + 计划缓存 | bench 首 run 即测，不等全部完成 |
| R-b | 绑定/步列表语义漂移（LIKE/NULL/collation 细节） | 中 | 叶语义调现有 eval 函数（03 红线）+ R1/R2 逐条单测 | 差分任何一行不等 |
| R-c | schema_version 机制引入并发 bug | 低 | 与 manifest version 同源传播；并发 DDL 回归 | catalog 并发测试 |
| R-d | CbfSource 搬运走样（去重/deletes 细节） | 中 | 逐行 diff review + 差分含段重叠用例 | I-H1 红 |
| R-e | 范围蔓延（顺手做向量化/并行/归并） | 高 | 里程碑"不包含"条款硬约束；每步独立 PR | review 纪律 |
| R-f | 双求值器漂移（prepared AST 直评 vs 步进） | 中 | 06 §1.1 过渡条款：差分语料必含 prepared 对拍；prepared 迁移不得无限期拖延 | 差分红 |
| R-g | 工作量低估 | 高（已发生） | 估算已按 2.5× 重估；里程碑拆更细 | 进度 |
| R-h | prepared 陈旧绑定（模板化引入的失效义务，评审 M1）| 中 | exec_prepared 入口重校验 (schema_version, branch)（06 §1.1）| 失效回归测试：DDL 后执行 prepared 必须重编译 |

## 3. 待决问题（**2026-09-15 全部按建议案定案**，开工指令已下；后续翻案须走 ADR 重开条件）

| # | 问题 | 选项 | 建议 |
|---|------|------|------|
| Q1 | schema_version 推导方式（06 §3 口径问题）| a) manifest diff 扫描 b) DDL 路径显式 bump | **b**（侵入小、语义准）|
| Q2 | Project 位置限制（02 §5-1）| a) 任意位置 b) 仅末端+谓词内 | **b**（缩小等价风险面）|
| Q3 | Like 预解析时机（03 §8-1）| a) v1 即编译 b) v2b.1 | **a'**：v1 保持运行时匹配不动，仅占位 Builtin——等价性优先 |
| Q4 | Chunk sel 策略（04 §6-1）| a) sel 跨算子 b) 相邻即物化 | **先 b**，bench 后再议 a |
| Q5 | Chunk 批大小上限（04 §6-3）| 2048 / 4096 / 行组对齐 | **4096**，bench 后调 |
| Q6 | force_source 实现位置（05 §6-2）| 派发器参数 vs 会话变量 | **派发器参数**（会话变量仅测试构建暴露 SET 解析）|
| Q7 | L2 缓存键含分支（06 §5）| 键加 branch vs 条目记录校验 | **键加 branch**（v1 简单）|
| Q8 | Delta 尾巴硬上限 | a) 无界（现状）b) 超限强制 checkpoint | **v1 无界 + bench 观测尾巴占比**，超 20% 查询走 Main+Delta 再议 |
| Q9 | 事务内派发（05 rule 2）| a) 保守 RowFallback（行为变化需豁免）b) v1 事务查询保留 if-else 不进派发器 | **b**（零行为风险；事务内 AP 派发进 v2c-2）|
| Q10 | LIKE 语义（新功能）| 转义规则（% _ escape）/大小写敏感默认值 | 仿 PG：敏感 + ESCAPE '\\'，Builtin 表条目独立 PR |
| Q11 | prepared 迁移时机 | a) v2b 内完成（B6）b) 过渡双求值器 | **a**（R-f 风险；工作量已计入）|
| Q12 | 多列 pk 既有 CBF 去重 bug | a) v1 顺手修 b) 单列已知问题，差分豁免 | **b**（等价迁移不夹带 bug 修复；bug 单独 PR）|
| Q13 | DML 走向（评审 P1-4）| a) v1 进派发器 b) 不进+差分看护+M-DML 后置 | **b**（11 号 spec 定案）|
| Q14 | digest 规整化（MySQL statement digest 同构）| a) 仅统计（top-N 积累，将来代价模型输入）b) 兼作 L1 缓存键 | **a**（三大系统都只做统计不做缓存键——自动参数化有类型陷阱；保型回填+1024 字节截断若做 b）|
| Q15 | L1 分桶改造时机 | a) v2b 顺带 b) 观测到干扰再改 | **a**（每语句热路径全局锁，改分片是纯收益；MySQL query cache 移除史是教训）|
| Q16 | EXPLAIN 双格式（PG 生态工具消费文本）| a) 仅 IR b) IR + PG 风格树形摘要层 | **b**（工具生态/DBA 熟悉度；摘要层是 printer 的第二输出，成本低）|

## 4. 演进后置项（明确不在本 spec 范围）

- morsel 多线程管线（AP 并行）；多列 pk 的 Main 路径；跨层 join；
  统计/learned 代价；eval_chunk 列式直线段（03 §5 v2）；
  表级 schema_version 粒度；memtx 行格式对齐 chunk 列式布局。

## 5. 评审检查单（合并前逐条打勾）

- [ ] 06 §3 的 schema_version 口径问题有实现方案
- [ ] 07 §4 验收线写进 bench 断言（不是文档承诺）
- [ ] 07 §2 差分基建先于 C2/C4 合入
- [ ] 三个前置小 PR（§0）先行合入
- [ ] Q1-Q12 逐条拍板并回写本文档
- [ ] 09 号文本表示：golden 文件更新纪律写进 AGENTS 检查单（09 §8-2）
- [ ] 每个里程碑独立 PR、独立可回滚
